# ARL 容器职责与任务流程分析

## 1. 结论先看

- AI 去噪执行容器：`arl_worker`
- AI 去噪执行队列：`arlweb`（由 `arl_task_web` 消费）
- `arl_web` 容器不跑 Celery worker，只负责 Web/API（Gunicorn）与容器内 Nginx
- 当前架构下，`arl_worker` 同时承载 `arltask/arlheavy/arlweb/arlgithub` 四类队列，容器总体负载通常高于 `arl_web`

---

## 2. 容器职责拆分

| 容器 | 主要职责 | 关键进程 |
|---|---|---|
| `arl_nginx` | 外部入口、基础认证、反向代理到 `arl_web` | `nginx` |
| `arl_web` | Web 前端资源 + API 服务 | `nginx`（容器内） + `gunicorn app.main:arl_app` |
| `arl_worker` | 异步任务执行（扫描/爬虫/漏洞/AI去噪/GitHub等） | 4 组 `celery worker` |
| `arl_scheduler` | 定时调度器（监控任务、周期任务下发） | `python3 -m app.scheduler` |
| `arl_rabbitmq` | Celery Broker 队列 | RabbitMQ |
| `arl_redis` | 缓存/辅助状态 | Redis |
| `arl_mongodb` | 任务与资产数据存储 | MongoDB |

---

## 3. `arl_worker` 当前承载内容（重点）

`arl_worker` 启动脚本会同时拉起 4 个 worker（同一容器）：

- `-Q arlgithub`
- `-Q arlheavy`
- `-Q arlweb`
- `-Q arltask`

对应并发来自配置：

- `CELERY_GITHUB_WORKER_CONCURRENCY`
- `CELERY_HEAVY_WORKER_CONCURRENCY`
- `CELERY_WEB_WORKER_CONCURRENCY`
- `CELERY_TASK_WORKER_CONCURRENCY`

### 3.1 队列与任务类型

| 队列 | 典型任务 | 说明 |
|---|---|---|
| `arltask` | 常规域名/IP任务、风险巡航、FOFA、资产任务（默认） | 默认主队列 |
| `arlheavy` | 重扫描任务（如端口量/目标量/服务识别条件触发） | 扫描重活隔离 |
| `arlweb` | Web 重阶段任务（截图/爬虫/目录/nuclei/afrog/WIH/渗透测试）+ AI 去噪任务 | 当前 AI 去噪都在此队列 |
| `arlgithub` | GitHub 搜索与监控任务 | 与资产扫描隔离 |

### 3.2 AI 去噪为什么在 `arl_worker`

- AI 去噪任务由 Celery action `AI_DENOISE_TASK` / `AI_DENOISE_MODULE_TASK` 执行
- 入队入口统一走 `arl_task_web.delay(...)`，即进入 `arlweb` 队列
- `arlweb` 队列消费者在 `arl_worker` 容器中

所以 AI 去噪一定在 `arl_worker`，不是在 `arl_web`

---

## 4. `arl_web` 当前承载内容

`arl_web` 主要承担：

- API 接口：任务管理、资产查询、AI 管理、导出报告等
- 前端静态资源分发
- 与 MongoDB/Redis/RabbitMQ 的读写交互（通过 API 触发任务和查询状态）

`arl_web` 不直接执行扫描/去噪计算型任务，不消费 Celery 队列。

---

## 5. 全链路流程（整体任务）

```mermaid
flowchart TD
  U[用户/前端] --> N[arl_nginx 入口认证与反代]
  N --> W[arl_web: Gunicorn API]
  W --> M[(MongoDB: task/asset/result)]
  W --> R[(RabbitMQ: Celery queues)]

  S[arl_scheduler 定时调度] --> R

  R --> Q1[arltask queue]
  R --> Q2[arlheavy queue]
  R --> Q3[arlweb queue]
  R --> Q4[arlgithub queue]

  Q1 --> WK[arl_worker]
  Q2 --> WK
  Q3 --> WK
  Q4 --> WK

  WK --> SCAN[扫描阶段执行: 域名/IP/站点/目录/漏洞/WIH...]
  SCAN --> AID[AI 去噪增量任务入队 arlweb]
  AID --> WK
  WK --> M

  M --> W
  W --> U
```

---

## 6. 单任务流程（以 Domain/IP 任务为例）

> 说明：阶段名会因开关不同而变化，下图是主干。

```mermaid
flowchart TD
  A[创建任务] --> B[submit_task 选择队列 arltask/arlheavy/arlweb]
  B --> C[Celery 消费执行]
  C --> D[主扫描阶段]

  D --> D1[domain_brute / dns_query_plugin / arl_search / alt_dns]
  D --> D2[port_scan / ssl_cert / cert_query_plugin]
  D --> D3[find_site + WebSiteFetch]
  D --> D4[site_spider / site_identify / site_capture]
  D --> D5[file_leak / nuclei_scan / afrog_scan / web_info_hunter]
  D --> D6[poc_run / weak_brute / penetration_test / findvhost]

  D2 --> T1[触发 AI 模块 cert]
  D4 --> T2[站点落库后触发 AI 模块 site]
  D4 --> T3[site_spider 后触发 AI 模块 url]
  D5 --> T4[file_leak/nuclei/web_info_hunter 触发 fileleak/url/nuclei_result/vuln]
  D6 --> T5[poc_run/weak_brute/findvhost/penetration_test 触发 vuln]

  T1 --> Q[入队 arlweb]
  T2 --> Q
  T3 --> Q
  T4 --> Q
  T5 --> Q

  Q --> AI[AI 去噪流水线按模块执行并落库 ai_denoise_result]
  D --> F[主扫描任务状态 done]
  AI --> G[AI 去噪可继续运行至 done/done_with_error]
```

---

## 7. 单个 AI 去噪任务流程（模块级）

```mermaid
flowchart TD
  S[某阶段结束触发 enqueue_ai_denoise_for_stage] --> M1[解析模块列表]
  M1 --> C1{ai_denoise_status 是否 queued/running}
  C1 -- 是 --> P1[合并到 pending_modules]
  C1 -- 否 --> Q1[创建 AI_DENOISE_MODULE_TASK 消息到 arlweb]
  Q1 --> R1[arl_worker arlweb 消费]
  R1 --> R2[run_task_ai_denoise_pipeline 按 modules 执行]
  R2 --> DB[(写入 ai_denoise_result)]
  R2 --> C2{是否还有 pending_modules}
  C2 -- 有 --> Q2[再次入队 pending_flush]
  C2 -- 无 --> E[结束]
```

---

## 8. 负载分析（当前架构）

### 8.1 为什么 `arl_worker` 容易高负载

- 所有异步执行都在一个容器内完成（4 队列共存）
- `arlweb` 队列既承载 Web 重阶段，又承载 AI 去噪
- AI 去噪改为“阶段增量触发”后，任务响应更快，但 `arlweb` 消息密度更高

### 8.2 `arl_web` 的负载特征

- 主要是 API QPS、查询聚合、导出接口、前端静态资源
- 不承担扫描和 AI 去噪计算，因此 CPU 峰值通常低于 `arl_worker`

### 8.3 可操作优化建议

- 优先调 `CELERY_WEB_WORKER_CONCURRENCY`，因为 AI 去噪在 `arlweb`
- 若端口重扫多，调 `CELERY_HEAVY_WORKER_CONCURRENCY`
- 若普通任务积压，调 `CELERY_TASK_WORKER_CONCURRENCY`
- 必要时拆分部署：将 `arlweb` 队列单独到专门 worker 容器

---



