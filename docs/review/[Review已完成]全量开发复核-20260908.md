# 全量开发 Review（2026-09-08）

## 1. 结论

- 复核基线：`1d333586`（当前 `newUI` 分支）；本轮提交未执行 `git push`。
- 复核范围：计划 1–7、附录 D/E、WIH 调度专项、统一发现系统边界、前端 daisyUI 主题与系统监控入口。
- 开发结论：**代码和开发文档已收口，可以归档为开发完成状态**。
- 发布结论：**真实目标、生产 runtime smoke、40/64 性能对照和默认开关切换仍未执行**，不能把本报告当作生产放行证据。
- `docs/plan` 现行计划文件已统一使用 `[开发完成]` 前缀，不再使用 `[未完成]` 或 `[已暂停]` 表示开发状态；计划 5 的生产统一切换暂停、计划 6/7 的真实验收待部署均在正文中单独标注。

## 2. 本轮实际修复

| 范围 | 修复 | 验证 |
|---|---|---|
| 计划 5 服务指纹 | 恢复既有 Nmap `http-proxy → http` 兼容别名，避免扩展服务基线造成旧扫描结果规范名回退 | 服务指纹产物重新生成；计划 5 门禁通过；新增 canonical 回归 |
| 测试卫生 | 为 `test_console_dashboard_trend` 提供 `app.routes.service` 最小 fake，修复单文件运行时的相对导入失败 | 独立运行 `6` 项通过；hygiene load-fail 减少 1 项 |
| 计划状态 | 计划 00、计划 5、计划 7 的状态文字统一为“开发完成 / 生产放行待部署” | `docs/plan` 无 `[未完成]`、`[已暂停]` 残留 |

## 3. 自动化验证

- `python3 scripts/plan567-code-check.py --plan all`：计划 5、6、7 离线代码回归全部通过。
- 计划 5 最新门禁：站点 `17`、服务 `15`、服务检测 `3`、构建 `16`，合计 `51` 项通过；服务产物为 `nmap_npoc_builtin_v1`、`47` 条规则。
- 前端 `npm run lint`：通过。
- 前端 Vitest：`19` 个 test files、`118` 项通过。
- 前端 `npm run build`：生产构建通过。
- `python3 scripts/check-theme-contrast.py`：6 套主题、12 组对比度全部通过。
- `python3 -m unittest ARL.test.test_theme_contrast`：2 项通过。
- 侧边栏激活项铺满修复后的前端回归：TypeScript、Vitest `19/118`、生产构建均通过。
- `bash -n ARL/docker/worker/start_worker.sh`、`git diff --check`：通过。
- 当前宿主测试卫生：`scanned=169 polluted=0 load_fails=60 clean=109`；60 个 load-fail 均为宿主未安装 `xing` 的环境依赖项，不将其伪装成全量通过。标准容器的 `149/149 clean` 证据仍以计划 3 已登记的容器回归为准，新的部署级复验不在本轮执行范围。

## 4. 计划状态矩阵

| 计划 | 开发状态 | 未执行的外部门禁 |
|---|---|---|
| 1 | 开发完成 | 运行环境凭据注入和标准容器证据由部署方复核 |
| 2 | 开发完成 | 真实任务运行、性能和发布证据由部署方复核 |
| 3 | 开发完成 | 40/64 目标、生产 runtime smoke、最终发布证据 |
| 4 | 开发完成 | 双架构 UI smoke、Safari、视觉和 Lighthouse |
| 5 | 开发完成 | `SITE_FINGERPRINT_SOURCE=unified` 生产切换按决策暂停；真实 Mongo/x86 对照未执行 |
| 6 | 开发完成 | 40/64 三组对照、端到端 p95、production runtime smoke、默认开关评审 |
| 7 | 开发完成 | 真实多目标、双架构和性能验收 |
| 附录 D/E | 开发完成 | x86 放行和计划 6 发布验收未执行 |

## 5. 归档口径

当前可以把这些文档归档为“开发完成方案”，继续作为边用边修阶段的实现和回滚依据；不能把它们归档为“生产放行已通过”。后续若执行真实验收，应在新的 revision、最终镜像和脱敏证据目录下追加发布验收记录，不覆盖本次开发结论。

## 6. 2026-09-09 复核补充（当前 HEAD）

- 当前复核基线：`13625739`（`newUI`）；本地工作区干净，未执行 `git push`。
- 侧边栏选中态、指标文字裁切、共享面板对齐、服务结果回退和任务结果展示等本轮 UI/数据展示修复仍保持在当前 HEAD；侧边栏最新修复已确认选中背景由整行容器统一承载。
- 前端最新门禁：`tsc --noEmit` 通过，Vitest `21` 个 test files、`130` 项通过，生产构建通过。
- `bash ARL/docker/frontend-src/scripts/ui-smoke.sh`：源码构建、入口资源自洽和 Docker 静态快照一致性通过；Docker 双架构 serve 因当前宿主 Docker 不可用而跳过，Playwright 因未安装而跳过。
- 本次复核发现并修复了版本 bump 后 `docker/frontend` 静态快照仍停留在旧版本 hash 的问题；当前 `ARL/version.txt` 与源码 dist、Docker 静态快照均为 `v4.7.413`。
- `python3 scripts/plan567-code-check.py --plan all` 及主题对比度门禁保持通过。宿主 Python 合跑仍受 `xing` 未安装和测试模块注入隔离影响，不能宣称宿主全量后端测试通过；相关容器证据和真实扫描验收仍按外部环境执行。
- 本补充不改变原发布结论：真实扫描观察期、40/64 目标、双架构发布验收、production runtime smoke、计划 5 生产统一切换及计划 6/7 默认开关评审仍未完成。
