# 前置收口记录(2026-09-05)

对应《计划 1–5 前置复核报告》§7"进入计划 6 前的建议顺序"第 1-4 项的执行记录。
第 5 项(计划 5 暂停)为决策沿用,无代码动作;第 6 项为门禁时点约束,记录在计划 6。

## 一、逐项处置

| 项 | 提交 | 内容 | 验证 |
|---|---|---|---|
| 1. 计划 1 旧 WAF 语义 | `ed38e8c0` + `90164e15` | 删除 `ACTIVE_BYPASS_MODULES`/bypass 记账/summary 绕过字段与 BOT UA 池;`test_policy_edit` 旧 `waf_bypass` 断言改负向钉 | 全量本地删除前后计数一致;行为经桩加载单独断言 |
| 2. 统一任务终态/候选 drain/metrics 覆盖 | `39f6e247` | 新增 `task_finalizer.py`:DONE 前有界 drain(复用 WihOrchestrator 幂等入口)、残余未取用主机写账本 `pending_backlog|wih|<host>`、证据经 StageExecutor 独立 stage metric;接线 Domain/IP/WebSiteFetch 三个编排器;`discovery_queue` 增加 `has_untaken/untaken_hosts`(deque 是观测镜像不收缩,残余以未取用为准);`domain_brute/arl_search/alt_dns` 手工 elapsed 改经执行器 | `test_task_finalizer` 14 项(含轮次区分、账本幂等、异常不阻断、配置开关) |
| 3. 资产监控接共享上下文 | `1e56f037` | `asset_wih_monitor` 每轮创建 `monitor|<scope_id>|<run_id>` DiscoveryContext,贯穿 URLFinder/页面情报/API 文档/JS 情报/敏感二次五阶段;`asset_site_monitor` 站点抓取由裸 `utils.http_req` 改 `fetch_text`+上下文;两处轮末输出脱敏 metrics 观测日志;run_wih(Go)/trufflehog(外部进程)边界显式注明 | `test_asset_monitor_context` 3 项(ctx 单例贯穿、轮次隔离、同站二次比对仅 1 次真实请求) |
| 4. 端口扫描配置收敛 | `33949a3e` | 漂移根源是 compose env 层静默压过挂载 YAML:删除 docker-compose 中 `ARL_PORT_SCAN_ALL_TARGET_BATCH_SIZE/BATCH_CONCURRENCY` 三服务 6 行重复声明,runtime YAML 为单一声明源(Python 默认与 example 同值 1/4 兜底);`config.py` 加载链末尾输出 `EFFECTIVE_SCAN_CONFIG` 白名单日志(仅数值/布尔键,无敏感项) | `test_port_scan_config_convergence` 4 项;本地部署文件 `config-runtime.yaml`(不入库)已同步 1/4,服务器侧需同步修改 |

## 二、验证口径

- 全量本地:`python3 -m pytest test/ -q --continue-on-collection-errors` → **124 failed / 369 passed / 34 errors**;
  failed 集合与收口前基线逐项一致(均为既有容器依赖缺失与顺序污染),34 errors 集合逐项一致。
- 本轮新增测试文件在**单文件与全量两种运行方式下均零失败**;新测试统一采用
  "`__path__` 桩包 + 收集期捕获真实模块引用 + sys.modules 与包属性双钉桩 + 源码正则取默认值"
  模式免疫既有 fake 模块污染。
- 容器门禁:前置 2/3 涉及真实任务链路(编排器、监控),本机 `xing` 缺失部分用例仅能桩验证;
  合入后需在标准容器执行计划 3 回归与监控任务实跑各一轮(记录于计划 6 第 3 批验收前)。

## 三、收口过程中新发现(移交下一轮 Review)

1. `docker/ai/skills/ai-pen-orchestrator/` 目录名携带旧渗透命名(连字符形态),内容为现行
   AI 去噪证据/路由参考文档——属**命名残留**而非执行语义;报告 §5 的 `ai_pen|AI_PEN` 检索式
   覆盖不到 `ai-pen`。建议更名目录并核对加载方引用,或显式登记为允许保留项。
2. 既有用例的模块级 fake 注入(`app.config`/`app.utils`/`app.services`)在本地 collection
   失败路径下不还原,是全量运行时若干 FAILED(如 `test_web_info_intel` 5 项)的顺序性根源。
   不影响容器门禁,但会持续制造"本机不可信"噪音;建议单独立项做测试卫生收敛。
3. `docker/config-runtime.yaml` 含真实第三方平台密钥且被 gitignore 正确排除;任何自动化
   改动该文件的会话都应注意它绝不能进入提交,且日志/输出不得回显其中凭据值。
