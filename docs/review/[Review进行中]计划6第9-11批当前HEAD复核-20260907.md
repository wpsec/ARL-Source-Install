# 计划 6 第 9–11 批当前 HEAD 复核

## 1. 复核结论

- 复核基线：`5e3de511`（2026-09-07），复核开始时工作区无未提交代码变更。
- 第 9 批 WAF/流量类别隔离和第 10 批 Rust 纯数据层的主体实现已具备，当前默认仍为 `API_UNIFIED_ENABLE=false`、`RUST_ACCEL_API_UNIFIED_MODE=shadow`。
- 未发现新的 P0 数据越界、凭据外泄或默认路径误切换问题。
- 当前不能发布或切换默认。amd64 真机回归与 Rust 基准已执行，但第 11 批的 40/64 目标对照、端到端性能和完整发布流程仍未形成完整证据；production runtime smoke 仅完成前置缺陷捕获与修复，完整 compose 健康检查仍待部署方 `.env`。实现/契约不一致项已由 T11-0/T11-1 处置并在 §10 分状态裁定。

初审判定（历史结论）：**Request Changes，继续第 11 批；不启动计划 7，不恢复计划 5 生产切换。** T11-0/T11-1 的当前裁定见 §10；计划 7 仍不启动，计划 5 仍不恢复生产切换。

## 2. 已核实通过项

- `host_in_scope` 统一范围闸、注册前闸、fetch 前二次闸已覆盖文档/Endpoint/浏览器/桥接路径；空范围 fail-closed 语义有测试。
- WAF 阻断可写入任务账本并在 Context 重建时恢复；类别级阻断与主机级阻断已分离。
- Endpoint claim 有 token fencing、lease 到期回收、finally 回收和 stale report 拒绝。
- URL 三层契约已按原始观测值保留，`parent_target` 脱敏误改已回退。
- Rust/Python 统一 corpus 严格顺序比较通过；Rust `cargo test --locked`、native smoke 和双 corpus 证据已登记。
- 定向验证：统一 Rust/API 套件 `47` 项通过、`7` 项因宿主依赖跳过；`test_site_fingerprint_registry` 独立进程 `15` 项通过；`test_asset_monitor_context` 独立进程 `3` 项通过。
- 复核过程中没有读取运行期敏感配置内容，也没有把凭据写入本文档。

## 3. 必须处理的发现

### P1-01：Rust 全局 `rust` 模式会误启用未过 CPU 闸的 stage

证据：`ARL/app/services/rust_accel.py:383-388, 484-496, 587-605`。

计划 6 第 10 批已明确 `normalize/method` 过闸，`hint/dedupe` 不过闸、不升级；但 `_unified_batch` 只按全局 `RUST_ACCEL_API_UNIFIED_MODE=rust` 判断，安全子集会统一走 native。当前没有按 stage 的启用门禁。

影响：未来把全局模式切到 `rust` 时，`hint/dedupe` 可能进入未达性能门槛的 native 路径，文档中的“不过闸不升级”无法由代码保证。当前默认 `shadow`，所以尚未形成生产行为变更。

要求：增加 stage allowlist 或等价的硬门禁。建议 `rust` 模式只允许显式列入的 stage 采用 native，未列入的 stage 保持 shadow/基线；测试必须证明 `hint/dedupe` 在全局 rust 下仍不会采纳 native。

### P1-02：`api_stage_network_wait_time` 计时范围与文档不一致，并可能重复累计

证据：

- `ARL/app/services/api_candidate_registry.py:770-798` 在调用整个 `fetch_text` 前后计时。
- `ARL/app/services/web_info_intel_utils.py:212-247, 258-290, 291-309, 311-353` 表明该区间包含缓存、singleflight、DNS policy、scheduler 等待、响应写入和真正的 `http_req`。
- `ARL/app/services/api_candidate_registry.py:748-750, 1483-1494` 使用累计 `network_wait_sec`，而 `DiscoveryContext.record_metric` 在 `ARL/app/services/discovery_context.py:1057-1059` 是累加语义。

影响：网络等待指标会高估真实网络时间；同一 Queue 实例多次 `run()` 时，累计值再次 flush，可能重复计数，无法作为第 11 批性能对照依据。

要求：只在实际 `utils.http_req` 调用区间计时；缓存命中、singleflight follower、DNS、调度等待和响应写入计为 0。每次 `run()` 只 flush 本次增量，正常返回、空范围提前返回和异常退出均要收口。补“同一 Queue 两次 run 不重复计数”测试。

### P1-03：`API_ENDPOINT_CLAIM_LEASE_SEC` 已写入契约，但没有标准配置接线

证据：

- `ARL/app/services/api_candidate_registry.py:586-597` 只从 Context 字典或 `Config` 属性读取，失败后回退 `900`。
- `ARL/app/services/api_unified_models.py:119-137` 的 `UNIFIED_API_CONFIG_DEFAULTS` 没有该键。
- `ARL/app/config.py` 当前有 Rust mode 的类默认、YAML 和环境变量接线，但没有 `API_ENDPOINT_CLAIM_LEASE_SEC` 对应接线。
- 计划 6 和紧急修复文档均把该键描述为“可配”。

影响：生产运行时通常只能使用代码常量 900 秒，运维无法按标准 YAML/环境变量调整 lease；文档承诺与实际配置能力不一致。

要求：把该键加入统一默认表、`Config` 类默认、runtime YAML、环境变量（建议 `ARL_API_ENDPOINT_CLAIM_LEASE_SEC`）和正数校验；补默认值、非法值回退和实际加载测试。

### P2-01：`degraded/host_waf_blocked` 的原因没有稳定进入 Endpoint Registry 资产面

证据：`ARL/app/services/wih_endpoint_probe.py:347-357` 在 probe item 上设置 `degraded_reason`；`UnifiedApiEndpoint` 位于 `ARL/app/services/api_unified_models.py:586-620`，没有对应字段；`ApiCandidateRegistry.probe_report`（`ARL/app/services/api_candidate_registry.py:651-678`）只把 `degraded` 映射为状态。

影响：legacy probe 记录可以保留原因，但 Registry 资产快照只看到 `degraded`，无法稳定区分主机级 WAF、其他降级和待处理状态，与计划中“`degraded/host_waf_blocked` 可显影”的表述不完全一致。

要求：明确原因字段的归属。建议在 Endpoint 资产面增加受控枚举 `degraded_reason`，限制长度和取值，快照/观测测试锁定 `host_waf_blocked`；不把完整响应或请求值带入该字段。

### P2-02：第 11 批引用的“附录 D”指向不清

计划 6 多处把 amd64 真机验收写为“附录 D”，但当前仓库可见的 `docs/plan/[未完成]05-附录D-x86放行runbook.md` 是计划 5 指纹生产切换 runbook，不是计划 6 API/Rust 双架构验收手册。

影响：开发和发布人员无法确定 40/64 目标、Rust mode、Endpoint 集合和端到端 ≤5% 的实际执行步骤，容易把计划 5 的指纹门禁误当作计划 6 证据。

要求：新增计划 6 专用发布验收 runbook，或把计划 6 的链接改为明确的专用文件；runbook 必须包含 revision/image digest、架构、目标集、非敏感配置指纹、输出集合 hash、metrics 和回滚步骤。

### P2-03：阶段 metrics 不是所有退出路径都能收口

证据：`ARL/app/services/api_candidate_registry.py:1365-1375` 在无 `allowed_hosts` 时直接返回；阶段 metrics 的 flush 位于后续正常路径 `1481` 之后，没有统一 `finally` 收口。

影响：空范围和阶段级异常时，`api_stage_wall_time`、`api_stage_cpu_time`、`api_stage_network_wait_time` 可能缺失，导致“跳过”与“未观测”混淆。

要求：抽取幂等的 `_flush_stage_metrics()`，挂在 `run()` 的 `finally`；提前返回和异常路径都产出状态与零值/已累计增量，不能吞掉原始异常。

## 4. 测试证据解释

- 统一 Rust/API 定向套件通过，不代表双架构发布门禁完成。
- `site_fingerprint_registry` 与 `asset_monitor_context` 分别独立进程通过；同一进程合跑曾因宿主缺少 `xing` 和测试 fake 模块收集期交错出现错误。这是已有测试隔离问题，不能作为本轮产品回归，也不能被当作全量绿证据。
- 计划 3 已登记 ARM64 compose 全栈第四轮 `875` 项、`29F/122E/18S`，hygiene `149/149 clean`；amd64 真机全量回归为 `875/31F/123E/18S`，Rust 基准、native smoke 和双 corpus strict 已通过，hygiene 工具口径为 `148/149 clean`，`test.test_wih` 隔离直跑通过。失败/错误已归因网络依赖和既有合跑收集期交错；40/64 目标、端到端 ≤5% 和完整 production runtime smoke 仍未完成。

## 5. 下一步开发计划：计划 6 第 11 批

> 本节保留复核启动时的执行计划；T11-0/T11-1 已于 2026-09-07 完成并在 §8–§10 登记，当前剩余门禁以 §10 为准。

### T11-0：先修契约和证据入口

1. 处理 P1-01、P1-02、P1-03；P2-01、P2-03 同批补齐。
2. 新增计划 6 专用发布验收 runbook，修正计划 6、计划 3 和总计划中的链接。
3. 在代码门禁修复前保持 `API_UNIFIED_ENABLE=false`、Rust `shadow`，不修改默认生产路径。

### T11-1：Rust stage 门禁

- 明确 stage 级 effective mode：建议仅显式 allowlist 中的 stage 进入 `rust`，其他 stage 保持 shadow。
- allowlist 默认只纳入已达闸且经过真机复跑的 stage；`hint/dedupe` 默认不得采纳 native。
- 测试覆盖：全局 rust、单 stage native、未授权 stage、native 异常、fallback 关闭、metrics 与实际输出一致。

### T11-2：阶段计时和配置接线

- `fetch_text` 只暴露真实 `http_req` 挂钟，不能改变既有二元返回调用的兼容性。
- Queue 每次 run 按增量 flush，异常/空范围也收口；增加重复 run、cache hit、singleflight follower、WAF blocked、DNS skip 测试。
- 接通 `API_ENDPOINT_CLAIM_LEASE_SEC` 的默认表、Config、YAML、环境变量和校验。

### T11-3：40 目标协同回归

- 固定 40 个目标、同一 revision、同一非敏感配置指纹，分别执行 legacy、统一 shadow 和 stage-gated rust 对照。
- 输出 Endpoint 集合、method/api_type、sources、状态、WAF 分类、重复请求、cache hit、首批结果 p95 和阶段 wall/CPU/network wait。
- 门禁：无未解释的 Endpoint 减少、无新增重复请求、无 shadow mismatch、WAF 不连坐、失败原因可归因。

### T11-4：64 目标性能与双架构

- ARM64 使用已验证容器链复跑同一 64 目标基线；amd64 必须在真实 x86 主机执行，qemu ratio 不采信。
- 每架构至少两轮 off/shadow/stage-gated rust；记录 wall、CPU、真实 network wait、RSS、结果数量、fallback 和 mismatch。
- 只有满足“结果集合不减少、golden 严格一致、端到端恶化不超过 5%、CPU/吞吐门槛满足”的 stage 才能升级；hint/dedupe 未过闸则继续 shadow。

### T11-5：发布流程

按计划 6 §十三执行：

1. shadow 观测期；
2. 统一层双写并比较 legacy/Registry 集合；
3. 小范围任务启用并观察结果、重复请求、WAF、异常和敏感字段；
4. 证据齐全后才讨论 `API_UNIFIED_ENABLE` 默认切换。

任一架构或 40/64 目标证据缺失，保持默认关闭，不以本地单测或 qemu 数据替代。

## 6. 多 agent 分工建议

为减少冲突，按文件边界拆分：

- Agent A：`rust_accel.py`、Rust adapter 测试；只处理 stage allowlist 和 mode metrics。
- Agent B：`api_candidate_registry.py`、`api_unified_models.py`、`config.py` 及对应测试；处理 lease、degraded reason、stage metrics 收口。
- Agent C：`docs/plan` 与本 review 文档的 runbook/链接修正；不改生产代码。
- Agent D：只负责 ARM64/amd64、40/64 目标回归和证据归档；未经授权不改代码，发现失败先记录复现条件。

建议提交拆分：

- `fix(core): 收口统一API分阶段Rust门禁与指标计时`
- `fix(core): 接通Endpoint lease配置与降级原因`
- `test(core): 补齐计划6第11批回归门禁`
- `docs(core): 补齐计划6双架构发布验收手册`

## 7. 最终放行条件

P1-01～P1-03 有代码和测试证据；P2-01～P2-03 不再造成契约歧义；ARM64/amd64 smoke 和 native corpus 通过；40/64 目标报告可复核；端到端 ≤5%；生产 runtime smoke 完成；之后才允许进入默认切换评审。

## 8. 处置记录（2026-09-07，被复核方追加；P1/P2 复审判定权在复核方——已由 §10 裁定 Accept）

T11-0 全部六项按本文要求实施完毕，逐项映射：

| 发现项 | 处置 | 证据锚 |
|---|---|---|
| P1-01 stage 门禁 | `RUST_ACCEL_API_UNIFIED_RUST_STAGES` 白名单（类默认=已过闸+native 实测的 normalize/method；hint/dedupe 不入列）；白名单外 rust→shadow；metrics `requested_mode`/`stage_gate` | `TestUnifiedStageGate` 5 项：全局 rust 下 hint/dedupe `used_native=False`、输出=基线、stub 仍被双跑调用（mismatch 观察不缺席）；空表全拒；未知 token 忽略 |
| P1-02 network wait | `fetch_text(timing_out=...)` 只写真实 `http_req` 挂钟（成功/失败两路径都写；缓存/singleflight/DNS/调度等待不写）；Queue 只累计该值 | `TestFlushTextTimingOut` 3 项 + Queue 累计 1.25s/缓存 0 两断言；口径冻结附录A §4.20 |
| P1-03 lease 接线 | 默认表+Config 类默认+`_ARL_POSITIVE_INT_KEYS`（config.yaml+env 双环）+runtime yaml 正值表；非正数/非法回退常量 | `ConfigLeaseWiringTest` 5 项（含 claim deadline=now+30 实测） |
| P2-01 degraded_reason | `UnifiedApiEndpoint.degraded_reason` 尾位字段 + 受控枚举/64 截断/other 收敛（不携带响应/URL 值）；probe_report/mark_endpoint 透传；非 degraded 迁移清除 | `EndpointDegradedReasonTest` 3 项 + orchestrator `reported_reasons` 透传钉 + models 契约 3 项（快照 blob 含 `host_waf_blocked`） |
| P2-03 finally 收口 | `run()` try/finally→幂等 `_flush_stage_metrics()`；全部累计源水位增量（首次恒产出含 0）；`api_document_schema_diagnostics_total` 改插入累计计数 | `QueueStageFlushTest` 4 项（双 run 不双计/空范围 flush/阶段异常 flush 且异常传播/网络口径）；既有 waf-blocked flush 断言保持通过 |
| P2-02 runbook | 新增 `docs/plan/[未完成]06-附录E-计划6发布验收runbook-双架构与40-64目标.md`（revision/镜像 digest/目标集 sha/非敏感配置指纹/四类计数/集合 hash 对账/三组对照/回滚表/完成定义）；计划 6 三处"附录 D"误指向改附录 E | runbook 文件 + 计划 6 状态行/§十五登记 |

验证：api 九件合跑 309 项全绿；`api-unified-golden.py --check` 无漂移；`compileall`、`git diff --check`、hygiene（4 个改动测试文件 clean）通过；契约登记附录A §4.20（含 §4.19 重号修正）。容器面复核已完成（2026-09-07 arm64 compose 全栈第三轮）：Ran 871/29F/122E/18S，本批全部新增测试在全量 discover 下 0 失败（flush 用例构造纳入 `_safe_domain_fns()` 守卫，首跑未守卫时入既有交错桶、修正后复跑验证），`ServerSelectionTimeoutError=0`，hygiene 149/149 clean，镜像内 native smoke 与双 corpus `--run-native --strict-order` exit=0；登记见计划 3 第三轮条目。默认面：`API_UNIFIED_ENABLE=False`、`RUST_ACCEL_API_UNIFIED_MODE=shadow` 不变。

T11-3/T11-4/T11-5 属授权目标与真机窗口，按附录 E 手册执行；本文 §7 放行条件的前三项（P1-01~03 代码+测试、P2 契约无歧义）已具备，容器 smoke/双 corpus/hygiene 复跑通过（见上），amd64 真机基准已在计划 6 T11-4 节登记；剩余为 40/64 目标、完整 production runtime smoke 和发布流程（附录 E 手册）。

## 9. 复核轮 2（2026-09-07 追加轮，来源：复核贴文；处置记录：被复核方——已由 §10 裁定 Accept）

发现：P1（WIH 补探仍按 (url,method) 合并/回填，违反 P1-12 冻结键——同 pair 的 rest/graphql 或不同 input_signature 资产被错误 observed、WAF/失败/降级归因可回填错资产）；P2（timing_out 写失败 `except Exception: pass` 需 debug 留痕；`UnifiedApiEndpoint` 允许 covered+degraded_reason 非法组合）；文档（计划 6 部分"当前状态"日期与 lease §4.17 指向需刷新）。

处置（提交 `616cce02`）：
- `wih_orchestrator._registry_endpoint_followup`：`probed_pairs`→`probed_identities`（首轮登记返回的权威对象 `idempotency_key`）；claim 循环 `seen_probe_keys`→`seen_probe_identities`；探测 item 注入 `endpoint_key`（`wih_endpoint_probe` 全链路 dict 拷贝透传，`run_wih_endpoint_probe` 已验证）；结果 `by_pair`→`by_key` 按 identity 回映射——**结果缺 `endpoint_key` 时拒绝按 pair 猜测归因**（warning 留痕，资产留 queued 由 lease/finally 回收下轮重探）。observed 免探仅限同 identity（含首轮多来源合并命中既有资产的场景）。
- 模型不变量：`__post_init__` 中 `degraded_reason` 仅 `status=degraded` 保留（covered+原因构造期归零，与 mark_endpoint 迁移清除对称）。
- `fetch_text` timing 写入 except 补 `logger.debug`（不静默）。
- 测试（review 要求三场景全覆盖）：`EndpointProbeIdentityT111Test`——同 pair 双形态各自领取探测（断言 2 item + observed 不出现）、同 pair 不同签名结果按 identity 独立归因（A degraded+原因、B probed 无原因）、同 identity 首轮合并恰一次 observed；`test_invariant_reason_requires_degraded_status`；既有 pair 语义桩改为真实 identity 构造。orchestrator 14 项、models 42 项全绿；api 十一件合跑错误集与 stash 基线逐项相同（probe_cache 既有交错，非本批引入）。

容器复核完成（2026-09-07 arm64 compose 全栈第四轮，含 `616cce02`）：**Ran 875，29F/122E/18S**——与本批前基线（871/29F/122E）计数持平且 T11-1 新增 4 项测试在全量 discover 下 0 失败（identity/orchestrator 相关错误仅既有交错桶 test_task_orchestrator/test_web_site_fetch_orchestrator，逐名对账无本批新增）；`ServerSelectionTimeoutError=0`；hygiene **149/149 clean**；发布链镜像内双 corpus `--run-native --strict-order` exit=0。P1-12 契约的 Registry 侧（register/claim 键）与本轮编排侧口径一致。

## 10. 分状态结论（2026-09-07 终轮裁定；本文保持 `[Review进行中]` 直至发布验收完成）

本文覆盖的是**第 9–11 批整体复核**，非仅整改轮。状态拆分为二：

1. **T11-0 / T11-1 整改项：Accept。**
   - T11-0 六项（P1-01/02/03、P2-01/02/03，§8）与 T11-1 复核轮 2（identity 收口 P1 + 计时/不变量 P2，§9）经复核确认通过：代码整改可判定通过，定向测试 `14 + 42 + 74 + 33 = 163` 项全绿，arm64 compose 全栈第四轮计数与第三轮持平且新增测试在全量 discover 下 0 失败。
   - §1 的 Request Changes 为历史初始结论，保留不改写；其"3 个实现/契约不一致项必须先处理"的前置条件已由 T11-0/T11-1 满足。
2. **计划 6 第 11 批发布验收：进行中（整体不放行）。** 未完成清单：
   - T11-3：40 目标三组对照（legacy / unified+shadow / unified+stage-gated rust），同 revision/镜像/授权目标集/非敏感配置指纹；
   - T11-4：64 目标冷/热两轮仍未执行；amd64 真机回归与基准已在 §9 取证，qemu 仅作功能面，不替代真机证据；
   - 端到端阶段 p95 恶化 ≤5% 证据；
   - 生产 runtime smoke；
   - `API_UNIFIED_ENABLE` 默认切换决策（§十三流程：shadow → 双写 → 小范围 → 全量）。

在此之前的配置基线（已核实为当前默认）：`API_UNIFIED_ENABLE=False`、`RUST_ACCEL_API_UNIFIED_MODE=shadow`、`RUST_ACCEL_API_UNIFIED_RUST_STAGES="unified_normalize,unified_method"`。放行硬条件沿用 §7：Rust 与 shadow Endpoint 集合严格一致、legacy↔unified 差异逐条可解释、p95 ≤5%、WAF/降级状态正确（`degraded_reason` 资产面可归因）、两架构 smoke/hygiene/golden 全绿、完整 production runtime smoke 通过。计划 7 不启动、计划 5 不恢复；下一步不是扩展解析器，而是按 [附录 E runbook](<../plan/[未完成]06-附录E-计划6发布验收runbook-双架构与40-64目标.md>) 完成剩余发布验收。最新归档见 [计划 6 发布验收证据](<./计划6发布验收证据-20260907/README.md>)。

## 11. 真实扫描现场观察（2026-09-08，阶段性记录）

本节登记部署方在 Kali 测试服务器上的真实扫描观察，作为后续问题归因和修复输入。当前只收到阶段日志与运行时快照，**尚未收到任务 API 的最终导出，不能视为扫描完成或计划 6 正式放行证据**。观察期间未重启容器、未修改运行配置；扫描终态后在本节继续追加最终任务状态、结果集合和脱敏证据索引。

### 11.1 任务与阶段产出

- 任务标识：`6a9fb25b26ea0d16f7e5e796`；本轮为测试服务器上的真实探索性扫描，阶段日志显示输入目标规模为 `180`。
- `find_site`：阶段 `success`，`output_count=180`，`wall_clock_sec=54.846042`；候选检查日志为 `836` 个候选、`208` 个结果，`dns_policy_skipped=211`、`dns_policy_overridden=2`，`request_count=625`、`request_errors=417`。
- `fetch_site`：阶段 `success`，`input_count=180`、`output_count=122`，`wall_clock_sec=28.931828`。
- `site_spider`：阶段 `success`，`output_count=164`，`wall_clock_sec=50.411120`。
- `site_identify`：阶段 `success`，`input_count=180`、`output_count=0`，`wall_clock_sec=18.116190`；日志显示从 `122` 个可用站点中先选择 `30` 个进行 staged select。
- `file_leak`：阶段 `partial`，`input_count=180`、`completed_target_count=122`、`output_count=267`，`degraded_count=82`，`pending_count=110585`，`queued_count=122`，`end_reason=site_degraded`，`wall_clock_sec=703.098002`；阶段结束日志时间为 `2026-09-08 15:37:29`。该阶段统计是阶段结果，不等同于任务最终终态。

以上统计说明本轮已产生可用于分析的真实阶段数据，但 `request_errors=417`、`degraded_count=82` 和 `pending_count=110585` 仍需结合逐目标终态、WAF 分类、错误类型和最终收尾账本判断，暂不直接判定为产品缺陷或预期行为。

### 11.2 Worker 与 RabbitMQ 运行时证据

- `arl_worker_2` 容器快照为 `status=running`、`restart=0`；其启动时间为 `2026-09-08T06:47:50.835601733Z`。`docker top` 仍能看到两个 Worker 容器内的 `arlheavy` Celery 主进程及其子进程。
- 运行日志多次打印 `acks_late:False`；容器内配置确认 `CELERY_PREFETCH_MULTIPLIER=1`。因此 ACK 事件不能简单归因于 late ACK 或预取倍率大于 1。
- `2026-09-08 15:30:40`，`arl_worker_2.log` 出现：`PRECONDITION_FAILED - delivery acknowledgement on channel 1 timed out`，RabbitMQ 报告的 timeout 为 `1800000 ms`。
- RabbitMQ 快照曾观察到 `arlheavy` 有 `1` 个 consumer、`8` 个 ready、`2` 个 unacknowledged；随后快照变为 `0` 个 consumer、`10` 个 ready、`0` 个 unacknowledged。Celery `inspect ping` 同时只返回 `arlweb`、`arltask`、`arlgithub` 两个容器的节点，未返回两个 `arlheavy` 节点；`list_consumers` 也未发现 `arlheavy` consumer。
- 当前证据指向“`arlheavy` OS 进程仍可见，但其 Celery 控制/AMQP consumer 已不可用，消息滞留在 ready 队列”的运行时故障模式。它解释了队列积压和后续深度阶段可能无法调度，但仍需最终任务导出确认哪些目标受影响。
- `file_leak` 子进程在上述 MainProcess ACK 异常之后仍输出阶段结束日志，说明 MainProcess 的 AMQP 故障与已派发子进程的实际结束时间并非同一事件；后续修复不能只依赖容器 `running` 或单个子进程退出判断。

### 11.3 待扫描终态后补充的证据

1. 通过 `/api/task/` 获取该任务最终 `status`、`deep_scan`、`statistic`、`end_time` 和收尾账本；终态按 `done`、`done_pending`、`done_degraded` 识别，`stop/error` 单独归因。
2. 通过 `/api/wih_endpoint/` 导出 Endpoint、HTTP method、状态码、来源、响应大小和去重后的接口集合；原始目标、Cookie、Token 和完整敏感 URL 不进入 Review。
3. 补齐任务终态前后的 `stage_metric`、`task_finalizer`、`pending_backlog`、WAF/降级分类，以及 RabbitMQ `list_channels`、`list_consumers` 和队列最终快照。
4. 以本轮真实数据确认：`file_leak` 的 `site_degraded` 是否为预期止损、`pending_count` 是否正确显影、`request_errors` 是否主要来自目标网络/WAF，及 ACK 故障是否造成 Endpoint 或阶段结果缺失。

### 11.4 暂定修复输入（不在本轮扫描期间执行）

- Worker 启动脚本当前主要通过 Celery OS PID 存活判断进行监督；需要补充 consumer/控制面健康判定，并增加“进程存活但 AMQP consumer 消失”的回归测试。
- 渐进式深度任务当前固定投递 `arlheavy`；需结合终态数据决定队列不可用时的持久化、延迟重试或受控 fallback 语义，避免把 ready 积压误判为任务已接管。
- ACK timeout 的根因需以 channel/consumer 快照和任务阶段时间线继续确认；在根因确认前不通过单纯提高 RabbitMQ timeout 或重启容器掩盖问题。

本节后续补齐终态数据后，再按附录 E 的 40/64 目标门禁整理正式证据；在此之前维持 §10 的“计划 6 第 11 批发布验收进行中、整体不放行”结论。

### 11.5 旧运行镜像导致域名后置阶段确定性失败（2026-09-08）

部署方补充了一条后续扫描失败记录：任务名 `tmp`、目标 `lssrmyy.net`、Task ID `6a9fb25c26ea0d16f7e5e79c`，本地时间 `2026-09-08 16:14:30`，失败阶段为 `domain_deep / npoc_service_detection`。异常为：

```text
AttributeError: 'DomainPostProcessStageService' object has no attribute 'run_save_service_info'
```

调用链为 `domain_deep_task -> DomainTaskOrchestrator.run_deep -> start_poc_run -> DomainPostProcessStageService.run_poc`，旧实现从 `DomainPostProcessStageService` 直接调用不存在的 `run_save_service_info`。当前仓库 HEAD 的 `cb927f35`（提交时间 `2026-09-08 15:25:33 +0800`）已经将调用改为 `DomainNetworkStageService(task).run_save_service_info()`，并包含对应回归测试；因此本次现场证据指向**测试服务器仍运行修复提交前构建的 `arl:local` 镜像**，而不是目标 `lssrmyy.net` 的网络异常。

已有容器快照显示 `arl_worker_2` 于 `2026-09-08T06:47:50.835601733Z` 启动（约本地 14:47:50），早于上述修复提交；该时间线与旧镜像判断一致。只要任务选项触发 `port_scan`、`service_detection` 或 `npoc_service_detection`，旧镜像都会在该后置收尾点重复失败，继续发起同配置扫描不能形成有效的 Plan 6 完整终态证据。

本 Task ID 与 §11.1 已登记的 `6a9fb25c26ea0d16f7e5e796` 不同，不合并统计。该任务应登记为“部署版本不一致导致的确定性失败”样本；修复前产生的阶段结果仅作为故障定位证据，不能作为发布放行证据。后续需先保存任务终态、阶段指标、队列/consumer 快照和脱敏结果，再停止新增扫描，基于 `cb927f35` 重建并强制重建应用容器，最后用单目标 smoke 任务复验。

部署方随后提供的证据目录进一步确认了上述判断：服务器源码 HEAD 为 `db7422d85c3018b6be4e1d19ab87b65e808116c3`（`fix(docker): 自动生成内部数据库凭据`），两个 worker 容器内实际代码均仍为 `self.run_save_service_info()`，没有加载 `cb927f35` 的修复调用；`arl:local` 镜像创建时间早于修复提交。当前 RabbitMQ 快照为 `arlheavy consumers=1 messages_ready=4 messages_unacknowledged=2`，`list_consumers` 显示该 consumer active，说明 consumer 状态发生过恢复或切换，但仍存在积压，不能据此判定扫描链路健康。

本次证据目录中的 `arl_worker_1-logs.txt`、`arl_worker_2-logs.txt` 实际只记录了 `docker logs` 的 `unknown flag: --no-color`，不代表 worker 没有日志；部署环境的 Docker CLI 不支持该参数，真实日志需去掉 `--no-color` 后重新采集。RabbitMQ 输出中的 `Timeout: 60.0 seconds ...` 来自管理命令执行超时提示，不应与应用 ACK 超时异常直接合并。

去掉 `--no-color` 后的第二次采集已能读取容器日志，但过滤词包含通用的 `timeout`，命中了 `EFFECTIVE_SCAN_CONFIG` 中的 `PORT_SCAN_BATCH_TIMEOUT_SEC`、`PORT_SCAN_STAGE_TIMEOUT_SEC` 等配置字段，导致输出主要是配置快照；该段输出本身未直接提供新的 `AttributeError` 或 ACK 异常行。后续日志证据应使用精确异常关键词，并优先核对宿主机挂载的 `ARL/docker/logs/arl_worker*.log`。

### 11.6 宿主机日志确认重复后置失败与 ACK 超时（2026-09-08）

宿主机挂载日志已通过精确关键词完成核验。`arl_worker.log` 中相同异常至少出现在以下任务：`...e797`（15:19:21）、`...e79b`（15:21:25）、`...e798`（16:03:39）、`...e79d`（16:13:01）、`...e79c`（16:14:30）、`...e7a1`（16:23:01）和 `...e799`（16:54:46）。这证明不是单个目标偶发失败，而是旧运行版本对同类任务的确定性失败。

当前任务 `6a9fb25c26ea0d16f7e5e79c` 的时间线为：`16:13:05` 开始 domain deep，`16:14:30.804` 的 `npoc_service_detection` 阶段记录为 `status=success` 且 `input_count=0`，紧接着在 `16:14:30.805` 记录 `domain deep task failed`，异常仍为 `DomainPostProcessStageService` 缺少 `run_save_service_info`。因此故障点明确位于协议识别阶段之后的服务信息收尾调用，不应归因于 `lssrmyy.net` 的网络或 WAF。

此外，`arl_worker.log` 在 `15:49:40` 和 `16:45:05` 两次记录 `PRECONDITION_FAILED - delivery acknowledgement on channel 1 timed out`；这与旧代码后置失败是两个独立但叠加的运行时问题，均应保留为 Plan 6 故障证据。`arl_worker_2.log` 还记录该任务在 `14:59:43` 完成 preview 并投递到 `arlheavy`，说明任务确实进入过深度队列。

截至本次采集，继续扫描只会继续产生无效失败样本；应立即停止新增任务，保留现有日志和 RabbitMQ 快照，先将服务器源码同步到 `cb927f35` 或之后版本并重建应用镜像，再进行单目标 smoke。当前日志证据已足够，不需要为证明旧版本问题继续发起扫描。

## 12. 现场故障驱动整改收口（2026-09-08）

### 12.1 已闭环或已归档项

| 项目 | 当前判定 | 证据与边界 |
|---|---|---|
| `DomainPostProcessStageService.run_save_service_info` 缺失 | **源码级闭环；部署级待复验** | `cb927f35` 已改为通过 `DomainNetworkStageService(task).run_save_service_info()` 调用，并有 `ARL/test/test_domain_stage_services.py` 回归测试；服务器旧镜像导致的重复失败按 §11.5/§11.6 归档，不再作为目标网络缺陷追查。 |
| worker 进程存活但 AMQP consumer 消失 | **代码整改完成；真实运行待复验** | `ARL/app/celerytask.py` 新增 `_inspect_worker_queue_health()`；`ARL/docker/worker/start_worker.sh` 在启动后及运行期检查四个队列的精确 worker/consumer，连续 3 次异常才终止容器交由 Compose 重新注册。 |
| `domain_deep` 固定投递 `arlheavy` | **代码整改完成；真实运行待复验** | 深度消息先判断 `arlheavy` consumer；不可用时回退 `arltask`，并将实际队列与原因写入 `deep_scan`/`dispatch_queue`；恢复逻辑按账本中的实际队列读取 ready 数，避免误判。 |
| 旧镜像真实扫描样本 | **已归档，不计入发布验收** | §11 记录了阶段统计、旧版本调用链、ACK timeout 和队列快照；任务最终导出尚未形成，因此不用于证明 40/64、集合一致性或 p95。 |

### 12.2 尚未闭环项

- ACK timeout 的底层 channel 根因仍需新镜像运行时复验；不通过单纯提高 RabbitMQ timeout 关闭问题。
- 新镜像的 production runtime smoke、单目标 `port_scan/service_detection/npoc_service_detection` 收尾验证、worker consumer 健康日志和队列零积压快照仍待部署方完成。
- T11-3/T11-4 的 40/64 目标三组对照、端到端 p95 ≤5% 和 `API_UNIFIED_ENABLE` 默认切换仍未完成；当前默认 `API_UNIFIED_ENABLE=False`、Rust mode 为 `shadow` 不变。

### 12.3 本地验证记录

- `git diff --check`：通过。
- `bash -n ARL/docker/worker/start_worker.sh`：通过。
- `python3 -m unittest ARL.test.test_celery_recovery`：15 项通过，包含 consumer 缺失和 control-plane 异常判定。
- `test_progressive_domain_queue` 在当前宿主机未执行，原因是运行依赖 `xing` 不可导入；新增回退用例已落盘，需在 ARL 运行镜像内执行。

本节不改变 §10 的发布结论：计划 6 第 11 批仍为“进行中、整体不放行”。新镜像部署后，先按附录 E 做单目标 smoke 和运行时快照，再恢复 40/64 目标验收。
