# 计划 1–5 前置收口 Review 报告（2026-09-05）

## 1. Review 范围

本轮针对计划 1–5 的前置收口和计划 6 第 3 批启动条件进行只读审查，依据当前 HEAD `7173019a` 及其前置提交：

- `ed38e8c0`、`90164e15`：删除旧 WAF 绕过语义并修正负向回归。
- `39f6e247`：统一 `TaskFinalizer`、有界 drain、残余 pending 账本和收尾指标。
- `1e56f037`：资产监控入口创建并传播任务级 `DiscoveryContext`。
- `33949a3e`：收敛端口扫描配置声明源并输出脱敏 effective config。
- `7173019a`：前置收口记录和三项新发现移交。

本轮不修改业务代码、不重启容器、不读取或输出 `config-runtime.yaml` 等敏感配置内容。

## 2. 结论

结论：**Request Changes（需处理后再宣称计划 1–5 完成）**。

前置收口的代码和针对性测试已经形成有效增量，但仍有一项重要的结果语义不一致：收尾器可以输出 `partial` 和 `residual_total`，编排器却无条件把任务状态写成 `DONE`。这会让“有明确 pending 证据”与“任务已完整完成”同时成立，和计划 2/3 中“队列清空后才标记 done”的约束冲突。

因此当前状态应表述为：**计划 1–5 的前置收口部分完成，计划 2/3 的终态和最终验收仍未完成；计划 6 第 3 批可以继续做契约级实现，但不能跳过终态修复和容器门禁。**

## 3. 已确认通过项

- 工作区在审查开始时干净，当前提交链连续，未发现 `target/`、wheel 或缓存构建产物被 Git 跟踪。
- `TaskFinalizer` 已通过 `StageExecutor` 输出独立收尾指标，残余主机可写入 `pending_backlog|wih|<host>`，没有把积压静默当成空结果。
- 两个资产监控入口均创建新的任务级上下文；同一轮下游 URL/页面/API/JS/敏感扫描共享该上下文。
- Compose 已删除重复端口扫描环境变量；Python 默认、示例配置和受控 effective config 的口径已统一，effective 日志使用白名单字段。
- 本轮新增的 21 项收口测试单独运行通过；全量本地套件仍保留既有失败/错误基线，不能作为全绿发布证据。
- 计划 1 中活动代码的旧 `ACTIVE_BYPASS_MODULES` 语义已经删除；测试中的旧字符串属于负向行为钉，不能按普通执行引用计数。

## 4. 发现的问题

| 等级 | 文件/模块 | 问题 | 影响 | 最小修复建议 |
|---|---|---|---|---|
| [重要] | `ARL/app/services/task_finalizer.py:181-200`；`ARL/app/services/task_orchestrator.py:53-58,89-94` | 收尾器根据残余候选输出 `status=partial`，但 `DomainTaskOrchestrator` 和 `IPTaskOrchestrator` 随后无条件写入 `TaskStatus.DONE`。 | 有积压时任务仍显示完成，违背“深度队列清空后才 done”，调用方无法仅依据任务终态判断结果是否完整。 | 收尾器返回可消费的终态决策；残余为 0 才写 `done`，否则写入现有兼容状态中的 `pending/degraded`，并保留详细 stage metrics。新增三类编排器集成测试。 |
| [重要] | `ARL/app/services/task_finalizer.py:65-91`；`ARL/app/services/web_site_scan_stage_services.py:294-321` | 收尾器只对 `NewHostQueue` 做有限 WIH drain；目录扫描在一次 `_merge_new_host_targets` 中读取已发现主机，后续晚到候选没有统一的策略级 drain。 | 爬虫、WIH 或其他阶段在收尾前产生的晚到主机/API/目录候选可能留到下一轮，无法证明同一 Task ID 内所有策略都完成消费。 | 建立统一的候选消费账本和阶段 drain 协议，至少覆盖 WIH、目录扫描和 URL Probe；每种策略明确“已消费/待处理/失败/降级”终态，或在文档中明确下一周期语义并将其显示为 `pending`。 |
| [一般] | `ARL/app/services/asset_wih_monitor.py:99-143` | 监控下游 Python 阶段共享 `DiscoveryContext`，但原生 Go WIH 和 TruffleHog 仍在上下文外运行。代码注释已明确这是外部边界。 | 不能宣称监控全链路都共享响应缓存；Go 子进程可能再次请求同一页面，跨进程来源和响应复用无法由本仓库完全验证。 | 保留现有外部边界，但在指标中单独标记 `external_network=wih_go/trufflehog`；后续通过 Go 输入协议或 IPC 接入响应快照，未完成前不得计入“全链路一次请求”门禁。 |
| [一般] | `ARL/test/test_asset_monitor_context.py` | 现有测试验证上下文对象传播和单独的缓存复用，但没有覆盖同一 URL 跨所有监控策略只发生一次真实网络请求，也没有覆盖晚到候选的全链路消费。 | 连接了上下文不等于业务策略真的使用了同一响应；重复请求下降和候选不丢失仍缺集成证据。 | 增加可控 HTTP fake：同一页面依次经过 crawler、URLFinder、WIH parser、API parser 和 probe，断言真实请求数、缓存命中数、消费者集合和候选状态。 |
| [重要] | `ARL/docker/docker-compose.yml`、`ARL/docker/config-runtime.yaml`、`ARL/app/config.py` | 受跟踪模板和 Python 默认值已统一，但实际服务器的 ignored `config-runtime.yaml` 仍需人工同步；当前没有真实运行环境的 effective config 和启动 smoke 证据。 | 本地测试通过不代表部署实际使用相同批次并发；性能对照可能被运行期配置漂移污染。 | 在不提交运行期配置的前提下，手动同步服务器配置，采集脱敏白名单日志并保存配置校验结果；禁止把密钥、Token 或完整运行期 YAML 放入报告/Git。 |
| [重要] | `docker/ai/skills/ai-pen-orchestrator/` | 目录名仍含旧渗透命名，现有 `rg 'ai_pen|AI_PEN'` 规则覆盖不到连字符形态；根据收口记录，目录内容是现行去噪参考文档而非执行链路。 | 仓库清理结论不可由单一检索式复现，后续维护者可能误判为废弃执行器残留。 | 二选一：重命名为不含旧语义的中性目录；或在计划 1 的允许项中登记路径、内容用途和排除依据，并增加连字符/目录名检查。 |
| [一般] | 既有测试 fake 注入 | 部分既有用例注入 fake 模块或函数后未恢复，顺序运行时会污染后续用例；该问题已在收口记录中移交。 | 全量结果中的失败/错误不能稳定区分生产回归与测试环境污染，降低门禁可信度。 | 独立建立测试卫生任务：使用 `patch`/fixture 自动恢复 `sys.modules`、模块属性和函数替换；按隔离进程重新生成全量基线。 |
| [重要] | `docs/plan/[已暂停]05-计划5-指纹体系统一与服务识别重构.md` | 规范产物和 Registry 已有，但生产默认仍是 `legacy`，服务规则产物仍标记 `seed_v0`，生产切换按用户决策暂停。 | 计划 5 只能算“规范化和前置实现完成”，不能算“统一指纹体系上线完成”；计划 6 不能把其当作已完成前置。 | 维持暂停状态，补齐真实规则覆盖、运行时 matcher 质量和 legacy/unified 对照后再单独放行；未放行前在所有总计划状态表标记 `[未完成][按决策暂停]`。 |
| [一般] | `ARL/app/tasks/domain.py` 及剩余 stage service | 任务类仍保留兼容入口、部分阶段实现和结果生命周期耦合；当前拆分是增量式，不能表述为所有扫描 stage 已完成服务化。 | 后续接入统一 API 队列或新的策略时仍可能绕过上下文、账本和阶段预算。 | 计划 6 第 3 批之前先冻结服务入口契约；后续按 stage 逐项迁移，要求每项都经过上下文、异常回退、幂等和 metrics 集成测试。 |

## 5. 计划状态判定

| 计划 | 当前判定 | 说明 |
|---|---|---|
| 计划 1 | 前置清理主体 [已完成]，仓库卫生 [未完成] | 旧 WAF/AI 执行语义已清理；连字符目录命名、测试卫生和运行期敏感配置治理仍需收口。 |
| 计划 2 | 核心增量拆分 [已完成]，最终解耦 [未完成] | 上下文、收尾器、配置和多个 stage service 已落地；终态、统一 drain、外部边界和剩余 stage 所有权未闭环。 |
| 计划 3 | 前置批次 [已完成]，最终验收 [未完成] | 目标性能、容器全量回归、实际配置证据、amd64 发布和 64 目标门禁未完成。 |
| 计划 4 | 代码重构 [已完成]，联调验收 [未完成] | UI 代码面和构建验证完成；双架构 smoke、Safari、视觉、Lighthouse、全量 Query/DataTable 和测试基建仍挂账。 |
| 计划 5 | 规范产物 [已完成]，生产统一 [未完成][按决策暂停] | Registry 和 golden 已有，生产默认仍为 legacy，不能宣称统一识别已上线。 |
| 计划 6 | 第 1–2 批 [已完成]，第 3 批 [未完成] | 下一批为 `ApiCandidateRegistry`、`ApiDocumentQueue` 和 JS 发现文档当前任务回流。 |

## 6. 下一步建议

1. 先修正“残余队列与 DONE”终态语义，并补 Domain/IP/Web 三类编排器集成测试。
2. 再补统一候选 drain 和晚到主机/API/目录候选的状态回归。
3. 在 ARM64/amd64 容器中执行同口径定向回归、全量回归和启动 smoke；运行期配置只做人工同步和脱敏校验。
4. 单独处理 AI 目录命名和测试 fake 污染，完成后重新生成可复现的残留与测试基线。
5. 上述前置证据齐备后，实施计划 6 第 3 批；第 3 批不得绕过统一候选状态、阶段预算和 `pending/failed/degraded` 语义。

## 7. 复核证据与限制

- 定向复核命令：`cd ARL && PYTHONPATH=. python3 -m unittest -v test.test_task_finalizer test.test_asset_monitor_context test.test_port_scan_config_convergence`；结果为 **21 项通过**。
- 21 项新增收口测试已通过；本报告不把它们扩大解释为全量套件或真实扫描全绿。
- 全量本地套件的既有失败/错误集合按 [前置收口记录](<./[已完成]前置收口记录-20260905.md>) 登记，最终结论需在标准容器中复现。
- 未检查或输出任何运行期密钥、Token、密码或完整 `config-runtime.yaml` 内容。
- 本报告不创建提交、不包含部署验收结论；后续若修复问题，应对受影响测试和 Docker smoke 重新取证。

## 8. 处置跟进（2026-09-05 终态修复轮）

本报告 §6 建议 1/2/4 及 §4 重要项1/2、外部边界标记、目录命名已于同日修复轮处置：
done 家族终态兼容映射、策略级候选 pending 显影（下一轮周期语义口径）、
`external_network_*` 指标、`ai-denoise-reference` 重命名与连字符检索口径。
证据与遗留边界见 [终态语义与统一drain修复记录](<./[已完成]终态语义与统一drain修复记录-20260905.md>)。
容器全量回归、真实配置 smoke、测试 fake 卫生、指纹生产切换维持本报告原判定不变；
本报告正文按历史快照保留，不改写。
