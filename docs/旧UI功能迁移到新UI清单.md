# 旧UI功能迁移到新UI清单（只增不减）

- 更新时间：2026-03-09
- 旧版参考仓库：`/Users/eric.sy.wu/Documents/Github/ARL-Source-Install`
- 新版目标仓库：`/Users/eric.sy.wu/Documents/Github/newui/ARL-Source-Install`
- 迁移原则：旧功能不能减少；可新增增强功能；所有功能必须有真实后端能力，不允许空壳按钮。

## 1. 迁移总原则

- [x] 旧UI已有页面/按钮/搜索项在新UI中必须保留（位置可变，能力不减）。
- [x] 旧UI已有接口能力必须可用（可复用原接口或做兼容别名）。
- [x] 新增功能不能影响旧功能行为和数据结构。
- [x] 页面交互统一新UI风格（输入框、下拉、勾选、弹窗、按钮一致）。
- [x] 所有批量操作必须支持多选并给出成功/失败反馈。

---

## 2. 模块级功能清单

## 2.1 任务管理（`/task/`）

### 页面与交互
- [x] 顶部按钮：`添加任务`、`FOFA任务下发`、`全局查看`、`局部查看`。
- [x] `局部查看` 在 `全局查看` 旁边。
- [x] 搜索项：任务名、目标、Task_Id、状态、任务类型。
- [x] 批量按钮：`批量删除`、`批量停止`。
- [x] 批量导出按钮：
  - [x] `C段批量导出`
  - [x] `域名批量导出`
  - [x] `IP批量导出`
  - [x] `IP端口批量导出`
  - [x] `站点批量导出`
  - [x] `URL批量导出`
  - [x] `表格批量导出`
- [x] 列顺序中 `扫描进度` 必须在 `配置项` 之前。
- [x] 行内操作：`同步`、`导出`、`停止`、`删除`、`重启`。
- [x] 任务名可点击，点击进入该任务详情。
- [x] 目标可点击，点击进入该任务详情。

### 接口能力
- [x] `POST /task/`
- [x] `POST /task/batch_stop/`
- [x] `POST /task/delete/`
- [x] `POST /task/restart/`
- [x] `POST /task/sync/`
- [x] `GET /export/{task_id}`
- [x] `POST /batch_export/site/`
- [x] `POST /batch_export/domain/`
- [x] `POST /batch_export/ip/`
- [x] `POST /batch_export/ip_port/`
- [x] `POST /batch_export/cip/`
- [x] `POST /batch_export/url/`
- [x] `POST /export/batch`

---

## 2.2 任务详情（全局查看/局部查看/单任务点击进入）

### 顶部导航（带数量）
- [x] `站点 - n`
- [x] `子域名 - n`
- [x] `IP - n`
- [x] `SSL证书 - n`
- [x] `服务 - n`
- [x] `文件泄露 - n`
- [x] `URL信息 - n`
- [x] `风险 - n`
- [x] `C段 - n`
- [x] `nuclei - n`
- [x] `指纹统计 - n`
- [x] `WIH - n`

### 站点页
- [x] 搜索项：站点、主机名、标题、Web Server、状态码、标头、指纹、favicon hash、标签。
- [x] 列：序号、站点、标题、headers、finger、截图。
- [x] `headers` 需按可读格式展示，不能全部挤在一行。

### 子域名页
- [x] 搜索项：域名、记录值、类型、IP、来源。
- [x] 按钮：批量删除、导出子域名。
- [x] 列：序号、域名、解析类型、记录值、关联IP、来源。

### IP页
- [x] 搜索项：IP、端口、操作系统、域名、CDN、IP类别（内网/公网）。
- [x] 列：序号、IP、操作系统、开放端口、关联域名、CDN、Geo、AS。

### SSL证书页
- [x] 搜索项：IP字段、签发者名称、主题名称、SHA-1、使用者备用名称。
- [x] 列：序号、HOST、CERT（证书摘要可读化显示）。

### 服务页
- [x] 搜索项：服务、IP、端口、产品。
- [x] 列：序号、服务、IP端口、Product。

### 文件泄露页
- [x] 搜索项：URL、标题、状态码、body长度。
- [x] 列：序号、URL、标题、状态码、body长度。

### URL信息页
- [x] 搜索项：URL、标题、状态码、body长度、来源。
- [x] 列：序号、URL、标题、状态码、body长度、来源。

### 风险页
- [x] 搜索项：漏洞名称、类别、应用名、目标。
- [x] 列：序号、漏洞名称、类别、应用名、目标、凭证、发现时间。

### C段页
- [x] 搜索项：协议、主机、端口、目标。

### nuclei页
- [x] 搜索项：模版ID、目标、漏洞URL、漏洞名称。
- [x] 列：序号、模板ID、目标、漏洞URL、漏洞名称、漏洞等级、保存时间、验证命令。

### 指纹统计页
- [x] 搜索项：finger。
- [x] 列：序号、finger、数量。

### WIH页
- [x] 搜索项：finger。
- [x] 列：序号、finger、数量。

---

## 2.3 资产搜索（站点/子域名/IP）

- [x] 资产搜索主标签高亮状态正确（切子页不丢高亮）。
- [x] 三子页：站点、子域名、IP。
- [x] 站点：保留搜索项与导出站点、风险任务下发。
- [x] 子域名：保留导出子域名、批量删除。
- [x] IP：保留导出IP列表、导出关联域名、导出IP端口（有后端支持）。
- [x] 不保留无后端能力的空按钮（例如无实现的添加/删除标签）。

---

## 2.4 资产分组 + 资产监控

### 资产分组
- [x] 搜索项：资产组名称、资产范围、资产范围ID。
- [x] 新建资产分组表单：资产类别（IP资产/域名资产）、资产组名称、资产范围（支持多行）。
- [x] 行内操作：
  - [x] 添加资产分组范围
  - [x] 添加监控任务
  - [x] 添加站点监控任务
  - [x] 添加WIH监控任务
  - [x] 删除
- [x] 资产范围字段可读化展示（不能全部缩在一行）。

### 资产监控
- [x] 搜索项：名称、域名、资产范围ID。
- [x] 批量操作：批量删除、批量停止、批量恢复。

---

## 2.5 策略配置

- [x] 页面：新增策略按钮 + 策略名称搜索。
- [x] 列：序号、名称、描述、更新时间、操作。
- [x] 操作：任务下发、编辑、删除。
- [x] 任务下发表单：任务类型（资产侦查/风险巡航）、任务名称、目标（多行）。
- [x] 策略表单布局优化：可勾选项不要“一项一整行”导致冗长。

---

## 2.6 计划任务

- [x] 顶部：添加计划任务。
- [x] 搜索项：任务名称、目标、策略名称、计划类型、状态。
- [x] 批量操作：批量删除、批量停止。
- [x] 列：任务名、目标、类型、状态、策略、时间配置、上次运行时间、下次运行时间、运行次数、操作。
- [x] 添加计划任务表单：
  - [x] 名称
  - [x] 目标（多行）
  - [x] 计划类型（定时/周期）
  - [x] 定时显示开始时间；周期显示CRON输入框
  - [x] 策略下拉
  - [x] 任务类别（资产发现/风险巡航）
  - [x] 钉钉通知、推送钉钉知识库
- [x] 目标字段可读化显示（不能缩在一行）。

---

## 2.7 GitHub管理（任务）

- [x] 搜索项：任务名称、关键字、状态。
- [x] 列：任务名、关键字、结果数目、状态、开始时间、结束时间、任务id、操作。
- [x] 操作：停止、删除。
- [x] 任务名可点击进入该任务详情结果页。
- [x] 可隐藏内部ID列（保留任务id展示列即可）。

### GitHub任务详情
- [x] 搜索项：路径名、仓库名、内容。
- [x] 列：仓库名、路径、内容、提交时间、关键字。

---

## 2.8 GitHub监控

- [x] 添加任务表单：任务名、关键字、cron表达式、钉钉通知、推送钉钉知识库。
- [x] 搜索项：任务名称、关键字、状态（运行中/停止）。
- [x] 列：任务名、关键字、cron表达式、状态、运行次数、上次运行时间、下次运行时间、操作。
- [x] 操作：修改、停止、恢复、删除。
- [x] 任务名可点击进入监控结果详情页。

### GitHub监控结果
- [x] 搜索项：路径名、仓库名、内容。
- [x] 列：仓库名、路径、内容、提交时间、关键字。

---

## 2.9 指纹管理 / PoC信息

- [x] 指纹管理隐藏内部 `_id` 列。
- [x] PoC信息隐藏内部 `_id` 列。
- [x] 保留同步PoC能力并可见。

---

## 2.10 API管理 / 配置管理 / 钉钉集成

### API管理
- [x] 作为三方引擎API配置页（FOFA、Hunter、Quake、Zoomeye等）
- [x] 保存后持久化到配置文件并可回读。

### 配置管理（独立一级页）
- [x] 域名爆破字典配置（选择+上传）
- [x] 域名爆破并发
- [x] 组合生成域名爆破并发
- [x] 黑名单IP
- [x] 域名解析器
- [x] 与 `ARL/docker/config-docker.yaml` 对应

### 钉钉集成
- [x] 页面保存后刷新不丢失。
- [x] 机器人配置 + OpenAPI配置 + 测试接口可用。

---

## 2.11 仪表盘 / 系统监控

- [x] 仪表盘保留并可展示核心统计。
- [x] “实时日志”应优先展示扫描日志，不是系统启动日志。
- [x] 系统监控展示主机资源（CPU、内存、磁盘、网络）可用。

---

## 3. 接口兼容清单（旧路径兼容建议）

> 说明：若前端已全部切换新路径可不做兼容；但若存在旧路径调用或外部脚本依赖，建议提供兼容。

- [x] `/task/stop/` -> 兼容转发到 `/task/batch_stop/`
- [x] `/scheduler/stop/` -> 兼容转发到 `/scheduler/stop/batch`
- [x] `/scheduler/recover/` -> 兼容转发到 `/scheduler/recover/batch`
- [x] `/wih/` -> 兼容到 `/wih/stat/` 或保留明细+聚合双接口

---

## 4. 验收标准

- [x] 菜单功能：旧UI菜单对应能力在新UI全部可达。
- [x] 功能完整：旧功能无缺失，新功能不破坏旧行为。
- [x] 后端可用：所有按钮均触发真实接口并返回可用结果。
- [x] 数据可读：目标、headers、资产范围等复杂字段可读化显示。
- [x] 批量能力：多选、批量导出、批量停止/删除可稳定执行。
- [x] 部署验证：`./scripts/quick-build.sh` 构建后主流程无阻断。

---

## 5. 备注

- 本文档为“迁移执行清单”，开发和测试过程中应持续勾选更新。
- 若某项确认“有意下线”，必须在本文档中明确写明原因与替代方案。
