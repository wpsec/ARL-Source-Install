# 05 附录A · 指纹现状冻结清单（第1阶段取证）

- 生成命令：`python3 scripts/fingerprint-baseline.py`（本文件即脚本输出，禁止手改；规则文件/代码变更后重跑刷新）
- 基线 rev：`1f91d0b5`，生成时间：2026-09-04 20:54:26
- API 端点面冻结复用 [04-附录A](./04-附录A-API契约冻结清单.md)（`/api/fingerprint` CRUD/上传/导出、`/api/site`、`/api/service` 均在其中），本清单只覆盖指纹文件、调用条件与结果字段面。

## 一、规则文件基线（sha256 前 16 位）

| 文件 | 大小(B) | sha256 | 规则条目 | 说明 |
|---|---|---|---|---|
| `ARL/app/dicts/webapp.json` | 663499 | `b02598c2b4a4595a` | 1930 | wappalyzer 风格 dict；legacy `utils/fingerprint.py` import 期加载 |
| `tools/finger.json` | 4644874 | `2e62e97f6d8f3e54` | 28884 | 外部大源，unique cms=15511；start.sh→sync-fingerprint.sh 导入 Mongo |
| `ARL/app/dicts/kscan_fingerprint.json` | 754397 | `af5cc5356b6a71c0` | 7239 | `{{name,human_rule}}` 列表；KSCAN_FINGERPRINT_ENABLE 控制 |
| `ARL/app/dicts/kscan_fingerprint.local.json` | 1524248 | `2805c5947b1830c4` | 9673 | bundle 产物格式（meta.format=arl_fingerprint_bundle，{"format": "arl_fingerprint_bundle", "generated_at": "2026-03-21T17:58:15Z", "source_count": 2, "app_accept": 9673, "rul） |

## 二、规则质量画像（误报控制的现状基线，第2阶段生成脚本的对照输入）

- **webapp.json**：信号数分布(≤5截断) `{"1": 1361, "2": 401, "3": 130, "4": 34, "5": 4}`；零信号规则 **0**、仅 icon 规则 **0**。
- **tools/finger.json**：method `{"keyword": 28142, "faviconhash": 742}`，location `{"body": 22971, "header": 5725, "title": 188}`；keyword 总数 28142，其中长度<6 的泛化关键字 **811**；同 cms 多条目 Top `[["log4j2", 78], ["hikvision视频监控", 27], ["drupal", 25], ["microsoftexchange", 24], ["wordpress", 23]]`。
- **kscan**：字段分布 `{"title": 5435, "body": 3866, "response": 2134, "header": 1562}`；操作符 `{"=": 12821, "==": 96, "!=": 80}`（`!=` 即 excludes 语义）；单规则条件数分布 `{"1": 4624, "2": 1293, "3": 592, "4": 372, "5": 358}`；单条件+泛化字面量(长度<8 或纯数字/端口样式) **1057** 条；解析断裂待人工复核 0 条。
- **kscan_local**：字段分布 `{"body": 10643, "title": 5435, "header": 3882, "response": 2134, "icon_hash": 244}`；操作符 `{"=": 21918, "==": 340, "!=": 80}`；单条件泛化 **990** 条；解析断裂 0 条。
- **regex 规则存量：两 kscan 文件均无 `re:` 前缀正则形态（纯字面量 + ||/!= 布尔），第2阶段无锚点兜底桶压力≈0；** 新增 regex 通道时须按 §2.3.1 召回保障执行。

## 三、名称交叉（第6阶段 Kscan 迁移与第2阶段合并的输入）

- kscan∩local：**7023**
- local−kscan(local独有)：**1760**
- kscan−local：**0**
- webapp∩kscan：**699**
- webapp∩local：**1063**
- finger∩kscan：**3036**
- finger∩local：**4182**
- finger∩webapp：**1337**
- 交叉按名字归一化（小写+去符号+CJK 保留）统计；同名不同规则的内容级 diff 在第2阶段生成脚本中输出（冲突清单）。

## 四、加载路径与调用条件（代码锚点，行号会漂移、以函数名为准）

```text
### webapp.json 加载（legacy 站点匹配）
ARL/app/config.py:899:    web_app_rule = os.path.join(basedir, 'dicts/webapp.json')
"ARL/app/dicts/file_leak/file_top_3w_\345\246\202\346\236\234\351\205\215\347\275\256\350\277\207\344\275\216\344\270\215\345\273\272\350\256\256\344\275\277\347\224\250.txt":23562:druid/webapp.json
ARL/app/utils/fingerprint.py:95:web_app_rules = json.loads("\n".join(load_file(Config.web_app_rule)))
ARL/app/utils/fingerprint.py:101:    for rule in web_app_rules:
ARL/app/utils/fingerprint.py:104:        new_rule["rule"] = web_app_rules[rule]

### kscan 规则加载
ARL/app/config.py:762:    KSCAN_FINGERPRINT_FILE = os.path.join(basedir, 'dicts/kscan_fingerprint.json')
ARL/app/config.py:1681:    if y["ARL"].get("KSCAN_FINGERPRINT_FILE"):
ARL/app/config.py:1682:        Config.KSCAN_FINGERPRINT_FILE = y["ARL"]["KSCAN_FINGERPRINT_FILE"]
ARL/app/config.py:2377:    Config.KSCAN_FINGERPRINT_FILE = env_str(
ARL/app/config.py:2378:        "ARL_KSCAN_FINGERPRINT_FILE", Config.KSCAN_FINGERPRINT_FILE
ARL/app/config.yaml.example:418:  # KSCAN_FINGERPRINT_FILE: "/code/app/dicts/kscan_fingerprint.json"
ARL/app/config.yaml.example:419:  # KSCAN_FINGERPRINT_FILE: "/code/app/dicts/kscan_fingerprint.local.json"
ARL/app/services/fingerprint_cache.py:16:from .kscan_fingerprint import load_kscan_fingerprint_rules
ARL/app/services/fingerprint_cache.py:471:        kscan_rules = load_kscan_fingerprint_rules()
ARL/app/services/kscan_fingerprint.py:244:        str(Config.KSCAN_FINGERPRINT_FILE),
ARL/app/services/kscan_fingerprint.py:323:def load_kscan_fingerprint_rules():
ARL/app/services/kscan_fingerprint.py:336:    file_path = os.path.abspath(str(Config.KSCAN_FINGERPRINT_FILE).strip())
ARL/app/tools/build_fingerprint_bundle.py:12:- 生成后的文件可通过 KSCAN_FINGERPRINT_FILE 指向使用

### tools/finger.json 导入链
ARL/app/tools/build_fingerprint_bundle.py:21:from app.tools.import_fingerprint import parse_finger_json, load_check_expression
ARL/app/tools/build_fingerprint_bundle.py:43:        finger_map = parse_finger_json(file_path)
ARL/app/tools/import_fingerprint.py:6:1. 读取 /code/tools/finger.json（cms/method/location/keyword 格式）
ARL/app/tools/import_fingerprint.py:238:def parse_finger_json(file_path):
ARL/app/tools/import_fingerprint.py:342:    parser.add_argument("--file", required=True, help="path to finger.json")
ARL/app/tools/import_fingerprint.py:357:    finger_map = parse_finger_json(file_path)
scripts/quick-build.sh:324:        "tools/finger.json"
scripts/sync-fingerprint.sh:4:# 将 /code/tools/finger.json 导入数据库，保证容器使用最新指纹。
scripts/sync-fingerprint.sh:13:FINGERPRINT_FILE="${ARL_FINGERPRINT_FILE:-/code/tools/finger.json}"
scripts/sync-fingerprint.sh:19:HOST_FINGERPRINT_FILE="${ARL_HOST_FINGERPRINT_FILE:-${ROOT_DIR}/tools/finger.json}"
start.sh:147:# 启动后主动同步一次指纹，确保升级后运行中的容器使用最新 tools/finger.json

### fingerprint_cache（置信度/合并/Redis）
ARL/app/routes/assetSite.py:256:        from app.services.fingerprint_cache import split_fingerprint_result_items
ARL/app/services/__init__.py:39:from .fingerprint_cache import finger_db_cache, finger_db_identify, finger_db_identify_detail, \
ARL/app/services/fetchSite.py:13:from .fingerprint_cache import build_legacy_fingerprint_items, split_fingerprint_result_items
ARL/app/services/fingerprint_cache.py:331:    REDIS_KEY = "arl:fingerprint:rules:v3"
ARL/app/services/task_result_item_service.py:12:from app.services.fingerprint_cache import split_fingerprint_result_items
ARL/app/services/webAnalyze.py:9:from .fingerprint_cache import normalize_wappalyzer_fingerprint_items
ARL/app/tools/import_fingerprint.py:18:FINGERPRINT_REDIS_KEY = "arl:fingerprint:rules:v3"
ARL/app/tools/import_fingerprint.py:91:def refresh_fingerprint_cache():
ARL/app/tools/import_fingerprint.py:333:    refresh_fingerprint_cache()

### bundle 构建工具（第2阶段扩展对象）
ARL/app/tools/build_fingerprint_bundle.py:21:from app.tools.import_fingerprint import parse_finger_json, load_check_expression
ARL/app/tools/build_fingerprint_bundle.py:24:def build_fingerprint_bundle(file_paths, max_rules_per_name=30, max_total_rules=12000):
ARL/app/tools/build_fingerprint_bundle.py:130:    items, stats = build_fingerprint_bundle(
scripts/sync-fingerprint.sh:46:run_import_fingerprint() {
scripts/sync-fingerprint.sh:50:      "env PYTHONPATH=/code nice -n ${IMPORT_NICE_LEVEL} python3 -m app.tools.import_fingerprint --file \"${FINGERPRINT_FILE}\""
scripts/sync-fingerprint.sh:56:    "PYTHONPATH=/code python3 -m app.tools.import_fingerprint --file \"${FINGERPRINT_FILE}\""
scripts/sync-fingerprint.sh:125:    if run_import_fingerprint; then

### finger_candidates 字段写入
ARL/app/routes/assetSite.py:261:        merged_finger_items.extend(item.get("finger_candidates", []))
ARL/app/routes/assetSite.py:271:            item["finger_candidates"] = candidate_fingers
ARL/app/routes/assetSite.py:273:            item.pop("finger_candidates", None)
ARL/app/services/fetchSite.py:71:        finger, finger_candidates = split_fingerprint_result_items(merged_items)
ARL/app/services/fetchSite.py:74:        if finger_candidates:
ARL/app/services/fetchSite.py:75:            item["finger_candidates"] = finger_candidates
ARL/app/services/task_result_item_service.py:42:        merged_fingers.extend(item.get("finger_candidates", []) or [])
ARL/app/services/task_result_item_service.py:54:            item["finger_candidates"] = candidates
ARL/app/services/task_result_item_service.py:56:            item.pop("finger_candidates", None)

```

调用条件结论（对照代码核实，防口径漂移）：

1. **站点识别双路径并存**：`fetchSite.py` 同时依赖 legacy `utils/fingerprint.py`（import 期模块级 `json.loads(Config.web_app_rule)`，进程级一次性）与 `fingerprint_cache`（Mongo `fingerprint` + Redis 缓存 + kscan）。统一目标 = 消灭 legacy 路径。
2. **服务识别**：Nmap 端口发现 + 可选 `-sV`（service_detection 选项）；NPoC 智能门控在 `IPTask._build_sniffer_targets`（仅低置信度端口、target_set 去重、空集时 ≤300 非 80/443 兜底、`full_port` 为用户显式选项）；`DomainTask._build_sniffer_targets` 同型。
3. **第2/3/4阶段先行件（`services/kscan_fingerprint.py` 已实现，收编复用、禁止第二套）**：引号感知 `||/&&/!=/~=` 布尔解析 `_split_logic_expression`；regex 字面量锚点提取 `_extract_literal_from_regex`（`regex_no_literal` 拒绝原因）；单名规则数上限与总量上限；`_config_signature` 配置变化即缓存失效——即 §2.3.1 锚点提取与 Stage4 "内容 hash debounce" 的同型先例。
4. **结果字段落点（冻结面）**：site/fileleak 文档 `finger`（confirmed）与 `finger_candidates`（候选，已投产，消费方 assetSite 路由/前端列）；`npoc_service` 集合（`tasks/poc.py` 与 `ip.py` 写 insert）；service 集合 product/version/exe 等字段由 Nmap 解析产出。第2阶段起新增字段必须先进本清单。
6. **导出面**：`/api/export`、`/api/batchExport/*` 的 finger 列消费 `finger` 字段（不含 candidates），冻结不变。

## 五、配置开关现状

| 文件 | 键 | 值 |
|---|---|---|
| `config.py 默认` | KSCAN_FINGERPRINT_ENABLE | True |
| `config.py 默认` | KSCAN_FINGERPRINT_FILE | os.path.join(basedir, 'dicts/kscan_fingerprint.json') |
| `config.py 默认` | KSCAN_FINGERPRINT_NAME_PREFIX | "" |
| `config.py 默认` | KSCAN_FINGERPRINT_REGEX_FALLBACK | "literal" |
| `config.py 默认` | KSCAN_FINGERPRINT_MIN_LITERAL_LEN | 5 |
| `config.py 默认` | KSCAN_FINGERPRINT_MAX_RULES_PER_NAME | 30 |
| `config.py 默认` | KSCAN_FINGERPRINT_MAX_TOTAL_RULES | 12000 |
| `ARL/app/config.yaml` | KSCAN_FINGERPRINT_ENABLE | true |
| `ARL/app/config.yaml` | KSCAN_FINGERPRINT_NAME_PREFIX | "" |
| `ARL/app/config.yaml` | KSCAN_FINGERPRINT_REGEX_FALLBACK | "literal" |
| `ARL/app/config.yaml` | KSCAN_FINGERPRINT_MIN_LITERAL_LEN | 5 |
| `ARL/app/config.yaml` | KSCAN_FINGERPRINT_MAX_RULES_PER_NAME | 30 |
| `ARL/app/config.yaml` | KSCAN_FINGERPRINT_MAX_TOTAL_RULES | 12000 |
| `ARL/app/config.yaml.example` | KSCAN_FINGERPRINT_ENABLE | true |
| `ARL/app/config.yaml.example` | KSCAN_FINGERPRINT_NAME_PREFIX | "" |
| `ARL/app/config.yaml.example` | KSCAN_FINGERPRINT_REGEX_FALLBACK | "literal" |
| `ARL/app/config.yaml.example` | KSCAN_FINGERPRINT_MIN_LITERAL_LEN | 5 |
| `ARL/app/config.yaml.example` | KSCAN_FINGERPRINT_MAX_RULES_PER_NAME | 30 |
| `ARL/app/config.yaml.example` | KSCAN_FINGERPRINT_MAX_TOTAL_RULES | 12000 |

## 六、golden 对照语料（第2-3阶段验收前置）

- 位置：`ARL/test/fixtures/fingerprints/`（测试资产入 git，不属"生成产物不进 Git"禁令）。
- 构成：`responses.json` 12 个合成样本（覆盖 header/body/title/response/icon_hash/url + `&&`/`||`/`!=` 布尔组合）× 当前实现**三段链**输出快照：legacy `utils/fingerprint.fetch_fingerprint`（webapp）→ `finger_db_identify_detail`（kscan+Mongo空集）→ `build_legacy+split` 合并 → `normalize_wappalyzer_fingerprint_items`；快照 `golden_v1.json` 由 `ARL/test/test_fingerprint_golden.py` 生成并锁定，回归模式严格对比。
- 无在线 DB：golden 只跑纯匹配函数（Redis/Mongo 以 fake 替身，先例见 test_route_build_return_items 的加载器模式）。

## 七、待用户拍板

1. `kscan_fingerprint.local.json` 是否进生产：local 较主 kscan 文件独有规则 **1760** 条、重叠 7023 条；若进，以哪份为准（建议：bundle 已含双源合并且做过 dedupe/accept 统计，以 local 为准、主文件退役为源输入）。
2. tools/finger.json 17445 个 cms、28884 条规则经 sync 导入 Mongo 后与规范文件的关系（建议：Mongo 为用户真相源、finger.json 仅作首次导入源，见计划§Stage4 部署拓扑决策）。
