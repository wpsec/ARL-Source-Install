# 00 总计划：仓库收缩与 Rust + Python 架构改造

状态：**开发完成**；真实目标、生产 smoke、UI 兼容性和最终发布验收仍由用户 review。

## 目标

本轮改造按“三段式目标”推进：

1. 先做一次明确的仓库收缩，彻底移除已废弃的 AI 渗透测试链路及其遗留资产。
2. 再建立 Rust + Python 混合架构，把经过基准确认的 CPU 密集型数据处理迁移到 Rust。
3. 最后推进核心单体解耦，降低 `commonTask`、任务编排、路由直连 Mongo、配置中心和导出链路的复杂度。

Rust 加速链路固定为：

```text
Celery -> Python Orchestrator -> Python Adapter -> Rust 批处理模块 -> WihRecord -> Mongo
```

## 改造边界

本轮保留能力：

- 资产发现主链路
- `WIH`
- URL / JS 增强
- 导出能力
- AI 去噪
- `nuclei / afrog / NPoC` 当前生产能力
- AI 去噪的 PoC 结果复核场景（AI-POC 扫描链本身已于 2026-09-04 经用户决策整体移除，见 03 第五批）

本轮删除能力：

- 旧 AI 渗透测试执行链路
- 本地 MCP runtime
- AI 渗透外部工具白名单执行器
- AI 渗透知识索引
- AI 渗透专属路由、统计、重试、批量运行、导出、前端入口
- `tools/ai_pen_tools`
- `tools/poc`
- 旧 AI 渗透专属 SOP、skill、默认 YAML、测试

## 混合语言架构原则

- Rust 只负责无副作用、可批处理、CPU 密集型逻辑。
- Python 继续负责 Flask、Celery、Mongo、Redis、配置、任务生命周期、网络策略、AI、Playwright 和外部扫描器编排。
- Rust 不直接访问 Mongo、Redis、Celery、LLM、浏览器或外部扫描器。
- Rust 优先处理 URL/JS/HTML 文本提取、URL 归一化、范围过滤、去重、候选排序和指纹计算。
- 现有 Go 版 `tools/wih` 保持不变，不与本轮 Rust 热点迁移混合；是否迁移由后续基准单独决定。
- 所有 Rust 化必须通过结果一致性和性能基准门禁，不能以语言迁移替代瓶颈定位。

## Rust 构建约束

- 新增独立 `ARL/native/arl_accel` PyO3/maturin 模块。
- Rust API 使用批量输入，避免逐条 FFI 调用；Python 现有公共函数签名保持不变。
- 生产镜像同时支持 amd64 与 arm64，使用 release 构建并固定 glibc 兼容下限。
- 主生产 Python 运行时统一按 Python 3.10+ 规划；旧 `ARMWorker/Dockerfile` 的 Python 3.8 仅作为归档路径，不纳入本轮验收。
- 构建必须固定 Rust、PyO3、maturin 版本并提交 `Cargo.lock`；Rust 构建产物、`target/` 和 wheel 缓存不得进入仓库。
- 使用稳定 ABI 和 manylinux/等效兼容构建。PyO3 支持 `abi3` 多版本 wheel，maturin 支持 manylinux 与跨架构构建。[PyO3 构建与分发](https://pyo3.rs/main/building-and-distribution)、[Maturin 分发与跨编译](https://www.maturin.rs/distribution.html)

## 关键原则

- 本轮是收缩式改造，不做旧 AI 链路的新旧双轨并行。
- 删除项直接退场，不保留兼容接口和历史只读查询。
- 在进入核心架构重构前，先修复当前任务执行的性能、预算和统计基线。
- 性能顺序固定为：基线采集 -> Rust 接口抽象 -> Rust 实现 -> Python 接入 -> 架构解耦。
- Rust 加载失败或单批异常时，必须记录原因并按当前批次回退 Python；禁止静默降级。
- 先清理废弃能力，再做 Rust 热点抽取，不让 Rust 模块依赖待删除代码。
- 每一批必须可独立测试、独立回归、独立发布。
- 文档、配置、代码、前端入口和构建链必须同步收口，避免“功能已删但入口仍在”。

## 性能治理主线

本轮性能治理与语言迁移并行但分层验收，先解决外部 IO、串行阶段和任务状态不可见问题，再扩大 Rust CPU 热点范围。

- 扫描采用同一 Task ID 下的双层异步语义：发现阶段先规范化、聚合并落库，深度阶段进入可恢复队列；首批可用域名、IP、站点结果目标为 5 分钟内可见，最终 `done` 只表示深度链路已清空。
- 全任务不设置硬截止时间，但阶段、provider、批次、目标和外部程序必须有独立预算；任何失败都必须落为 `failed`、`degraded` 或 `pending`，不得用空结果伪装成功。
- `test/top100/top1000/custom` 严格使用用户选择的端口集合；`all` 严格完成 `0-65535`，可以先展示初筛结果，但不能用常用端口结果替代最终结果。
- 端口扫描在具备 `CAP_NET_RAW` 时优先使用 `nmap -sS`；权限不足只允许按当前批次明确记录原因并回退 `-sT`，不得静默改变扫描方式。`all` 模式按单 IP 批次执行，默认 4 路且不超过 4 路受控并发，并在每批完成后更新指标。
- 已确认属于 CDN 的 IP 默认跳过 IP 端口扫描，不把 CDN 边缘节点的 80/443 伪造成开放端口；站点发现仍基于业务域名执行 HTTP/HTTPS 探测。疑似“全端口开放”主机保留原始端口结果，但跳过高成本的逐端口服务识别并标记可疑状态。
- 站点探测仅对端口阶段已确认关联公网 IP 的业务域名，放行“解析器与系统解析均为公网但无交集”的负载均衡/DNS 视图漂移；私网地址、未确认关系和其它 DNS policy 失败仍拦截，并记录例外与降级计数。
- provider 先做全局候选规范化和去重，再按 provider 使用独立并发度、连接/读取超时、有限重试和熔断；代理仅作为配置后的兜底，端口扫描不经过 HTTP 代理。
- 来源聚合只去重结果，不丢失 `source/sources` 关系；批次失败、待处理数量和降级原因必须可追溯。
- 40 个目标用于定位 `port_scan`、provider、文件泄漏、WIH 和 Nuclei 的瓶颈；64 个目标使用相同配置执行 Rust 开关对照，作为最终 p95 和结果完整性门禁。
- 性能结论区分墙钟、CPU、网络等待、内存和结果数量；网络 IO 占主导时不强行归因于 Rust。

## 统一发现上下文与跨工具协同

扫描性能治理的第一优先级是消除工具之间的重复请求和数据断链，而不是继续扩大 Rust 覆盖范围。

- 每个 Task ID 建立一个 `WebAssetScanOrchestrator` 和一个 `DiscoveryContext`，由上层统一编排站点获取、爬虫、URLFinder、WIH、目录扫描和 URL 探测。
- `DiscoveryContext` 包含任务内热响应注册中心、候选资产注册中心、事件分发、来源聚合、WAF 流量隔离和幂等状态；持久化账本只保存可恢复状态与结果元数据，不能让 Redis 成为业务结果唯一事实源。
- 页面响应遵循“一次获取，多方消费”：爬虫、URLFinder、WIH、站点识别和 URL 探测优先消费同一任务内响应，不因不同工具再次请求同一 URL。
- 候选遵循“一次发现，全链路分发”：新页面、新 API、新 JS、新目录和新子域统一进入候选图，再按类型分发给相应策略，保留完整 `sources` 关系。
- Python 爬虫、URLFinder、WIH 和目录扫描仍是独立策略，不合并成无法隔离预算和失败状态的单体扫描器；共享的是响应、候选、调度和结果协议。
- 目录扫描使用独立流量类别和 WAF 熔断器。目录请求触发的疑似 WAF 只能暂停目录队列，不能连坐熔断正常爬虫和 WIH；只有确认主机级封禁时才暂停站点全部请求。
- 候选状态必须区分 `discovered`、`queued`、`fetching`、`fetched`、`covered`、`failed`、`degraded`、`pending` 和 `skipped`，不能用“出现过 URL”代替“已经完成内容处理”。
- Rust 暂不重写整个 WIH，仅在统一响应和候选数据流稳定后，继续作为 HTML/JS/URL 解析、过滤、排序、去重和指纹计算的后置加速层。

## 风险项

- `commonTask.py` 中 AI 渗透逻辑与 `AI-POC`、`nuclei / afrog` 编排存在历史耦合，删除时要严格区分保留与废弃路径。（2026-09-04 落地：AI-POC 链与僵尸 `penetration_scan` 模块整体移除，nuclei/afrog 回落默认参数路径，AI 去噪/WIH 填充不受影响。）
- `api_console.py`、`export.py`、`App.tsx` 中存在大量配置与展示耦合，必须前后端一并清理。
- 启动流程导入即执行副作用已收口：`arl_update` 移出 `app.main` 导入期，生产由 `start_web.sh` 在 gunicorn 前显式调用 `app.tools.arl_bootstrap`，开发直跑在 `__main__` 保留同语义；worker 导入路径本就不经过 `main.py`。
- Rust FFI 的批量数据转换、异常边界和多架构构建可能引入结果差异或部署失败。
- 仓库内存在历史构建产物与大体积资料目录，删除后要重新验证引用关系。

## 冻结规则

- 本轮计划以 `docs/plan/` 下 `00`–`07`（文件名带状态标签）为唯一现行方案族；各计划是否处于实施态以文件名标签和本节"当前状态索引"为准。计划 5 的开发文件已归档为 `[开发完成]`，仅生产统一切换按决策暂停；计划 7 开发侧已收口，真实验收仍不前置。
- `docs/history/[已归档]Strix-cn集成重构计划.md` 视为归档文档，不再引用为实施依据。
- `docs/plan/[开发完成]WIH提速与调度优化实施计划.md` 继续作为 WIH 调度专项方案，文档级引用方为 `02`（`03` 在实施记录中按同批事项处理，不单独链接）。
- 在完成第 1 阶段废弃能力清理前，不新增任何替代 AI 渗透链路。
- 本轮只对统一数据流中的纯数据热点做 Rust 加速，不直接重写 Go WIH；全 Rust WIH 方案暂停，后续是否恢复必须重新立项。

## 依赖顺序

1. 计划文档落盘并冻结清单和语言边界。
2. 完成任务执行性能、预算控制、WAF 统计和 WIH 可观测性修复，并冻结基线。
3. 删除旧 AI 渗透与废弃资产。
4. 建立 PyO3/maturin crate、Python adapter 和 amd64/arm64 构建门禁。
5. 迁移 URL/JS/HTML 热点并完成 Python fallback 和结果一致性回归。
6. 收口 README、配置模板、前端入口和测试。
7. 建立任务级 `DiscoveryContext`、统一响应/候选注册中心和请求调度器。
8. 接入站点获取、爬虫、URLFinder、WIH、目录扫描和服务类型路由，完成跨工具结果联通。
9. 进入路由、repository、orchestrator、stage executor、配置中心和导出链路的剩余解耦。

## 当前实施状态

阶段 7 已完成第一批任务仓储、WIH 编排、统一阶段执行器、导出查询仓储和配置持久化服务拆分。第二批及其后续增量已将扫描配置、第三方服务 API 配置、AI 配置的读取/校验/敏感字段保护/锁内合并/持久化分别收口到 `ScanConfigService`、`ServiceApiConfigService`、`AIConfigService` 和 `ConfigDomainService`，并将 DomainTask/IPTask 的任务终态与阶段生命周期统一接入 `TaskPipeline`；WebSiteFetch 阶段顺序已移至 `WebSiteFetchOrchestrator`。本批继续将域名发现、网络/IP、站点移交、站点外部扫描、WIH 和后置处理拆到独立 stage service，并将统计、C 段统计、指纹统计、资产同步和 `task_finalize` 收口到 `TaskLifecycleService`，任务类只保留兼容入口与上下文适配；任务对外接口、Mongo 字段和 Python 公共入口保持不变。新增 `AIPromptSopService`、`AIProviderTestService`、`ServiceApiProviderTestService` 和 `TaskResultItemService`，分别收口 SOP/Provider 测试和多类结果文档组装；新增 `WebSiteSingleStageService`、`IPSingleStageService`，可选阶段的回退必须显式记录 degraded。WAF 观测新增响应头/响应体分离匹配、主流厂商画像和严格 DNS CNAME 证据接口，厂商专属 CNAME 可参与 CDN/WAF 边缘 IP 跳过，但不把通用 CDN 字符串当作确定性 WAF。当前已在 macOS Apple Silicon 的 ARM64 Docker 环境完成 Rust native smoke、golden corpus、核心分层回归和完整应用镜像构建，并在 amd64 Docker 环境使用同一源码完成完整应用镜像、release wheel、native smoke、golden corpus 和仓储回归验证；实际生产发布部署验收以及 64 个代表性目标的冷启动/热缓存阶段级性能基线仍未完成，不得用历史粗粒度任务数据替代。共享发现上下文已完成首批接线：六类发现事件、候选资产图登记、爬虫/URLFinder/WIH 情报链/目录扫描的响应复用与流量类别调度已生效；目录字典流量的 WAF 证据只暂停 directory 队列并可从子进程回流，非目录来源维持主机级口径；调度采用有界等待后 fail-open，杜绝限流伪装空结果。候选图有容量上限（默认 20000，最旧驱逐并计数）；`DiscoveryLedger` 已接入 Mongo 后端（`task_stage_ledger`），文件泄漏、WIH 主扫描与 Nuclei 在 worker 重启重投后可跳过已完成目标（账本 fail-open，宁可重扫不可漏扫，仅全绿批次记账 covered）；普通 HTTP 路径连接池已收编（共享适配器 + 每请求独立 Session 保 cookie 隔离，直连/provider 分支待分池评审）。共享上下文内存态准入规则：有界、非唯一事实源、预算与驱逐可观测。

当前解耦仍是增量式，不宣称单体已经拆完：`api_console.py` 仍保留 AI 配置规范化和兼容代理；SOP 文件处理、AI/Provider 测试细节已移至独立 service。`IPTask` 的端口扫描、站点发现、NPoC 和弱口令配置，以及 `WebSiteFetch/CommonTask` 的站点获取、爬虫、截图、文件泄漏和站点识别已移至具体 stage service，任务类仍保留兼容入口；`DomainTask` 的域名构建、DNS 过滤、网络保存和证书兼容阶段已完成所有权迁移；结果字段组装已移至 `TaskResultItemService`，`CommonTask` 的统计与任务收尾已由 `TaskLifecycleService` 承载，Mongo 结果写操作已由 `TaskResultWriteService` 集中封装。证书处理、服务汇总已由 `IPCertStageService`/`IPServiceSummaryStageService` 承接，Nuclei/afrog 扫描由 `WebSiteNucleiScanStageService`/`WebSiteAfrogScanStageService` 承接，WIH 记录落库与风险提升由 `WihResultPersistService` 承接，AI-POC 规划器（常量、上下文/索引收集、模型调用、决策落地与用量日志）由 `AIPocPlannerService` 承接，任务侧仅保留兼容委托与常量别名；至此批次 12/13 的具名迁移项全部完成。（2026-09-04：`AIPocPlannerService` 随 AI-POC 能力整体移除，本句仅作迁移历史保留。）

2026-09-07 增量记录：WebSiteFetch 的结果/WAF/目标/发现上下文/Nuclei 延迟重试已完成服务化收口；Python Provider 与搜索引擎退避已接入阶段 deadline 硬截止。对应代码提交为 `53507d7a`、`db551e77`、`bad55af7`、`dfe40f40`、`58f19a59`、`4c8d6218`、`d5edcfb0`。2026-09-08 已完成 DomainTask 剩余阶段所有权、结果写入 Owner 和运行配置安全预检；40/64 目标和生产验收仍保持未完成状态。

2026-09-07 连接池增量记录：直连 IP 请求已按流量模块、直连 IP、SNI 和端口接入有界适配器池，每次请求仍维持独立 Session；`test_http_session_pool` 3 项通过，提交 `e113fbd2`。Go WIH/fileLeak 跨进程租约与缓存仍不在本次改造范围。

2026-09-07 UI 增量记录：ApiConsole 与 DingTalk 集成页面补齐 mutation 成功后的查询缓存失效、敏感配置 reveal 不误触发重取及 token 查询 key 跨实例隔离回归覆盖，提交 `8f1e58e1`。UI 容器 smoke、Safari/视觉/Lighthouse、Phase 3 全量迁移和最终发布验收仍未完成。

2026-09-07 UI 主列表记录：`TableModuleView` 主列表读取已切换为 `useQuery`，保留动作/分页的命令式刷新兼容入口，并补充同一 `QueryClient` 重挂载缓存复用回归；`TableModuleView` 页面测试 6 项通过，提交 `cf0e65ac`。Phase 3 全量迁移、DataTable 页面级接入和外部验收仍未完成。

2026-09-07 UI 弹窗记录：`ActionDialog` 的字典、策略和 PoC 选项改为直接派生 React Query data，仅保留表单默认值补齐副作用；TypeScript 检查和弹窗测试 3 项通过，提交 `958caa5f`。Phase 3 全量迁移、DataTable 页面级接入和外部验收仍未完成。

2026-09-07 UI 仪表盘记录：`DashboardView` 的聚合快照、资产趋势、最近任务和日志展示改为直接派生 React Query data，保留日志暂停与手动刷新交互；TypeScript 检查和现有 Dashboard 测试 5 项通过，提交 `38c6c97c`。Phase 3 全量迁移、DataTable 页面级接入和外部验收仍未完成。

2026-09-07 UI 工作台记录：`AiConsoleView` 的用量统计、场景选项、日志列表、总数、更新时间和错误信息改为直接派生 React Query data；`DingtalkIntegrationView` 的工作区/节点按需读取改为 React Query mutation，保留成功提示与调试结果回写。对应页面测试 4 项、6 项通过，提交 `b0e27e3f`、`8cc11172`。Phase 3 全量迁移、DataTable 页面级接入和外部验收仍未完成。

2026-09-07 UI 配置记录：`ConfigConsoleView` 的字典选项、扫描档位、配置路径和更新时间改为 query 快照派生；保存、字典上传和 PoC 更新先合并响应到缓存再失效重取，保留表单编辑与成功反馈。该页测试 6 项通过，提交 `8867e31c`。Phase 3 全量迁移、DataTable 页面级接入和外部验收仍未完成。

2026-09-07 UI 配置批次记录：`ApiConsoleView`、`DingtalkIntegrationView`、`AiConsoleView` 的配置路径、更新时间、运行状态和 Provider presets 收回 React Query 查询快照；保存/SOP 上传成功后统一先合并响应到缓存再失效重取，保留敏感配置 reveal 的明文态隔离。三页配置查询重复本地镜像已清理；用户并行测试改动尚未合入。本批仅推进 Phase 3 代码面，UI 容器 smoke、Safari/视觉/Lighthouse、DataTable 页面级接入和最终发布验收仍未完成。

2026-09-07 UI 列表记录：`TableModuleView` 主列表 queryFn 与兼容 `loadRows` 共用取数函数和缓存 key；force refresh 同步标记列表/任务详情计数缓存，导出 job 轮询纳入独立 query key 并支持重入去重。现有模块测试 6 项通过，提交 `712bde9c`。Phase 3 全量迁移、DataTable 页面级接入和外部验收仍未完成。

2026-09-07 UI 主列表数据源记录：`TableModuleView` 的 `rows/total/loading` 改为直接派生当前 React Query 列表快照，模块生命周期缓存仅保留分页、筛选、排序和滚动位置等 UI 状态；列表同步 effect 与本地数据镜像移除，`loadRows` 保留为动作/分页的兼容查询入口。用户并行测试改动尚未合入。本批仍未完成 DataTable 页面级接入、UI 容器 smoke、Safari/视觉/Lighthouse 和最终发布验收。

2026-09-08 并行收口记录：Go WIH 已补私有输入 manifest、跨 worker lease 和短 TTL 成功结果缓存；fileLeak 子进程协议补版本、lease 及 `html_get` 输入/`file_leak_get` 输出 profile 边界；`DiscoveryEventConsumer` 已订阅并消费 `SiteDiscovered`/`PageFetched`，站点与页面候选分别进入目录和爬虫阶段；API 查询新增并校验 `classification`（`unknown|business|credential_like`）；`TableModuleView` AI 批量去噪迁移到 React Query。随后完成 DataTable 页面级主列表接入、首行冻结下线发布说明与兼容处理、运行配置安全预检、DomainTask/CommonTask 剩余阶段所有权迁移和任务结果写入 Owner 收敛。40/64 目标、生产 smoke、双架构 UI 和最终发布验收仍保留为集中收口项。

2026-09-08 开发复核记录：EvidenceGraph adapter 补齐 `documents`/`calls`/`references` 关系、参数与证据 ID 引用，并通过 14 项图谱定向回归；DaisyUI 主题解析与六套主题对比度门禁通过；系统监控摘要保留小图标、移除状态文字和状态点，并固定摘要区域及顶部操作栏宽度，监控指标文本防裁切，相关定向测试 11/11 通过；服务模块重挂载后仍展示 `service_info` 的 IP/端口和产品，相关回归 10/10 通过；IP 阶段服务边界测试改用轻依赖 bootstrap，独立 2/2 通过。前端当前为 19 个测试文件、118 项测试，`tsc`、production build 和静态 dist smoke 通过。对应本地提交为 `3dd3c40c`、`672b879d`、`4d2176d6`、`e5b3e2e4`、`334ee566`、`51af2032`、`9bf9807e`、`5d1f001b`、`b205283b`、`d8e88739`；真实 worker、40/64、多架构、性能和发布验收仍不提前宣称完成。

2026-09-07 核心收口记录：NPoC 服务识别的目标选择、协议规范化、双端口模型结果回填和检测服务兼容逻辑已移入共享 helper，`DomainTask/IPTask` 保留兼容委托；域名阶段与服务注册表离线回归 23 项通过，提交 `7b3598f3`。

代码增量已收口，后续执行顺序固定为真实环境验收：①归档 03 第四轮验收缺口(a)-(e)结论；②执行附录 E 双架构、40/64 目标、性能/集合/WAF/终态门禁；③完成 production runtime smoke 和发布回滚演练；④仅在上述证据齐备后评审默认开关。直连分支分池与 TruffleHog 外部边界按设计保留，不是当前开发阻塞；计划 5 的开发已完成但生产统一切换按决策暂停，计划 7 开发侧已收口。没有完成真实性能数据或目标架构 smoke 前，不将“代码已收口”表述为“发布门禁已通过”。

## 当前状态索引（2026-09-08 刷新；计划 1/2/4 行随本轮阶段与 UI 收口更新）

本节是当前状态的权威索引；前文历史实施记录保留，不因本节而改写。全量开发收口见 [全量开发 Review](<../review/[Review已完成]全量开发复核-20260908.md>)；详细问题见 [计划 1–5 前置收口 Review 报告](<../review/[已完成]计划1-5前置收口Review-20260905.md>)；Review §4 重要项1/2 与外部边界标记的处置结果见 [终态语义与统一drain修复记录](<../review/[已完成]终态语义与统一drain修复记录-20260905.md>)；计划 6 第 9–11 批最新状态见 [当前 HEAD 复核](<../review/[Review进行中]计划6第9-11批当前HEAD复核-20260907.md>)。

| 计划 | 状态 | 当前结论 |
|---|---|---|
| 计划 1：AI 渗透与废弃资产清理 | [开发完成] 主体 + 目录命名收口 + 测试卫生 + 运行期配置治理 | 活动旧 WAF/AI 执行语义已删除；`ai-pen-orchestrator` 已重命名为 `ai-denoise-reference`；运行期配置文件、凭据注入和无敏感值启动预检已收口，hygiene 容器面闭环。 |
| 计划 2：核心架构解耦与分层重构 | [开发完成] 增量拆分 + 终态映射 + 策略级 pending 显影 + DomainTask/CommonTask 阶段所有权 + 结果写入 Owner + 外部协议与事件消费 | done/done_pending/done_degraded 终态兼容映射、跨进程 lease/manifest、事件消费、阶段服务和结果写入统一均已落地；40/64 性能和真实发布证据由用户统一 review。 |
| 计划 3：实施批次与验收回归 | [开发完成] 前置批次、回归工具、配置预检和发布 runbook | arm64/amd64 构建、native smoke、双 corpus、阶段指标和基线工具已落地；40/64 目标实跑、production runtime smoke 和最终发布证据由用户统一 review。 |
| 计划 4：UI 体系重构 | [开发完成] 代码面 + DataTable 页面接入 + 首行冻结发布兼容 + Phase3 数据层迁移 | done 家族前端兼容、DataTable 主列表、滚动模型发布说明和数据型 React Query 迁移已完成；本地 19 文件/118 项 UI 测试、tsc、build 和 dist 自洽通过；双架构 smoke、Safari、视觉/Lighthouse 由用户统一 review。 |
| 计划 5：指纹体系统一与服务识别 | [开发完成] 运行时统一与离线门禁；[生产放行待部署][按决策暂停] 生产统一 | 默认仍为 legacy，service 产物为 `nmap_npoc_builtin_v1`；真实 Mongo/x86 对照和生产切换按决策暂停。 |
| 计划 6：统一 API 解析与 Endpoint Registry | [开发完成] 第 1–11 批 + 整改轮 1/2 + 紧急修复 T0-T6/R6 + 发布工具链 | 解析器链 OpenAPI→Postman→GraphQL→WSDL、Registry 消费方接入、阶段调度/WAF 类别隔离、Rust 纯数据层和发布验收工具链全部落地；`API_UNIFIED_ENABLE` 默认 False，真实目标运行和默认切换由用户统一 review。 |
| 计划 7：通用 Web/API 资产证据图与智能验证 | [开发完成] | 已复用计划 6 的 `ApiCandidateRegistry`/`ApiDocumentQueue`/`EndpointRegistry`，开发侧已接入目标画像、证据图、协议/微前端适配、HAR/代理事件、L0/L1/L2 受控验证和任务级回归；真实多目标、双架构和性能验收仍待部署。 |

当前开发状态：计划 1/2/3/4/5/6/7 的代码和开发文档任务已完成；计划 5 的生产统一切换按用户决策暂停。附录 D/E 的开发期 runbook 和离线门禁已完成，真实放行与发布验收仍待部署。后续由用户统一 review 真实环境、性能和发布证据；在 review 前不主动修改生产默认开关。
