# 01 计划1：AI 渗透与废弃资产清理

## 目标

彻底移除已放弃的 AI 渗透测试能力及其仓库残留，保留当前仍在使用的 `nuclei / afrog / NPoC / WIH` 主链路。

本阶段必须先于 Rust 热点抽取。Rust 模块不得引用已删除的 AI 渗透模块、配置、数据结构或历史结果字段。

## 删除范围

### 目录与资产

- 删除 `tools/ai_pen_tools`
- 删除 `tools/poc`
- 删除仅服务旧 AI 渗透的知识索引和生成物
- 删除旧 AI 渗透专属 SOP 和 skills

### 后端实现

- 删除 `/ai_pen_test` 全部路由与统计、重试、批量运行、删除逻辑
- 删除 `ai_pen_mcp_runtime`
- 删除 `build_ai_pen_knowledge_index.py`
- 删除 `commonTask` 中 AI 渗透专属执行、候选构建、外部工具调用、知识索引装载逻辑
- 删除任务、导出、统计、控制台等链路中的 `ai_pen_test_result` 读写与展示逻辑

### 配置与前端

- 删除 `config-docker.yaml` 中全部 `AI_PEN_*` 开关和路径
- 删除配置中心中所有 AI 渗透执行相关字段、接口和前端表单
- 删除前端 `AI渗透测试` 模块、统计面板、详情弹窗、批量操作和测试按钮

### 测试与文档

- 删除 `test_ai_pen_*`
- 删除仅验证旧 AI 渗透耦合逻辑的测试
- 删除 README 中 AI 渗透、外部工具目录、PoC 知识库说明
- 将旧 `Strix-cn` 方案改为归档说明

## 明确不做

- 不保留旧 `/ai_pen_test` 只读兼容
- 不保留旧历史查询接口
- 不保留旧 AI 渗透导出与报告章节
- 不为删除能力增加新的兼容层

## Rust 相关边界

- 不为旧 AI 渗透链路新增 Rust adapter、FFI 接口或替代执行器。
- 不把待删除的 AI 渗透逻辑作为 Rust 性能基准样本。
- 删除完成后，Rust crate、Python adapter、构建脚本和配置模板都必须通过同一套残留检索。

## 性能治理前置约束

- AI 渗透废弃链路清理必须先于 Rust 热点抽取和 adapter 接口冻结。
- 清理范围覆盖 Python、Rust、Docker、构建脚本、配置模板和测试；`rg 'ai_pen|AI_PEN'` 必须覆盖全仓，而不是只检查 Python。
- provider 异常、任务错误和阶段指标不得输出 API key、邮箱、Token、密码或带凭据的请求 URL；错误文本进入日志、Mongo 任务详情和 traceback 前必须脱敏。
- 清理完成前不得让 Rust 模块引用已删除的 AI 渗透模块、配置或结果结构，也不得把废弃链路纳入性能样本。
- AI 清理验收通过后，才允许冻结 Rust adapter 的输入、输出、fallback 和 metrics 语义。

## 与统一发现上下文的关系

- AI 废弃链路清理必须先于 `DiscoveryContext`、候选事件和 Rust 解析接口冻结，避免旧 AI 渗透字段进入共享候选图。
- 清理范围覆盖 Python、Rust、Docker、构建脚本、配置模板和测试；共享发现上下文不得引用已删除的 `ai_pen` 模块、状态或结果字段。
- provider、爬虫、WIH、目录扫描的异常日志必须沿用脱敏规则，不得因为统一事件记录而输出 API key、Token、邮箱、密码或带凭据 URL。
- `AI_POC`、AI 去噪和现行 WIH 辅助能力仍需与已删除的 AI 渗透执行链区分，不能把它们作为共享发现策略重新引入旧 runtime。（2026-09-04 修订：`AI_POC` 经用户决策已整体移除，本句仅对 AI 去噪与 WIH 接口辅助继续有效。）

## 实施注意

- `build_poc_index.py` 与 `AI_POC` 当前仍存在生产引用，若仍服务保留能力，则不纳入本阶段删除清单。（2026-09-04 修订：保留决策反转，`ai_poc_planner_service.py`、`build_poc_index.py`、`poc_index.json` 及 `AI_POC_SCAN_ENABLE` 配置已删除；同时僵尸化的 `penetration_scan.py`/`penetration_request_policy.py` 与 `strip_disabled_penetration_options` 强制关闭机制一并下线。）
- `nuclei_result`、`vuln` 中保留的 `ai_pen_*` 字段若仅用于旧渗透结论回填，需一并评估是否删除或清空展示。

## 当前清理状态（2026-09-03）

- 旧 `/ai_pen_test` 路由、MCP runtime、知识索引生成器、专属工具目录、SOP、skill 和对应测试已从工作区移除。
- 活动代码、配置、导出排序和前端源码中不再保留旧 AI 渗透测试入口；历史 CHANGELOG 与本计划中的迁移记录仅作为审计资料保留。
- 当前仍保留的 AI 去噪和 WIH 接口 AI 辅助属于现行结果降噪能力，不是旧主动渗透执行链，不能重新引入旧 AI 渗透 runtime 或外部工具执行器。（原句含 `AI_POC`，2026-09-04 起该能力已移除。）
- WAF 的 `penetration_test` 兼容开关仍由统一任务入口强制关闭；本批未把通用 Web 专项实现误判为旧 AI runtime，后续如需整体删除需单独确认范围。（2026-09-04 已完成该确认：penetration_test/waf_bypass 选项、执行链与强制关闭函数整体删除。）

## 验收条件

- 仓库中不再存在 `tools/ai_pen_tools`、`tools/poc`
- 后端不再存在 `/ai_pen_test` 路由与 `ai_pen_mcp_runtime`
- 配置模板、README、前端入口中无现行 AI 渗透说明
- Rust 模块、构建脚本和 Python adapter 中不再存在旧 AI 渗透引用
- `rg 'ai_pen|AI_PEN'` 只允许出现在归档说明、迁移注释或尚未处理的第二阶段待办中

## 前置复核结论（2026-09-05）

本计划的 AI-PoC、旧路由、旧工具目录和配置主体已清理，但验收尚未完全收口：

- `ARL/app/services/waf_guard.py:71` 仍保留 `ACTIVE_BYPASS_MODULES = {"penetration_test"}`；`ARL/test/test_waf_guard.py` 仍验证该旧模块的有限绕过行为。
- 该残留不等同于旧 AI 执行器仍在运行，但与本计划“`penetration_test/waf_bypass` 语义整体删除”的结论冲突，属于旧兼容语义未清理。
- 进入计划 6 前，必须删除该语义及对应测试，或重新定义为不属于旧渗透链路的独立 WAF 策略，并更新本计划的范围和验收规则。

详细报告：[计划 1–5 前置复核报告](<../review/[已完成]计划1-5前置复核报告-20260905.md>)。

### 阻断项处置（2026-09-05，用户决策：删除死语义）

- 已按上述最小处理建议执行删除：提交 `ed38e8c0`。`waf_guard` 的 `ACTIVE_BYPASS_MODULES`/bypass 判定与记账/summary 绕过字段、仅被绕过使用的 BOT UA 池整体移除；`prepare_request` 保留为无副作用直通（`conn.py`/`wih_endpoint_probe`/`wih_endpoint_ai_fill` 三处调用点兼容）；`commonTask`/`fileLeak` 去掉 `bypass_enabled` 参数与跨进程上下文键（全链无 `True` 路径，属双重死代码）。
- `test_waf_guard.py` 原绕过放行测试替换为删除后行为钉：封锁主机对 `penetration_test` 同样保守跳过、Header 无注入、summary 无 bypass 键。
- 全量本地套件删除前后计数一致（124 failed/348 passed/34 errors，error 集合逐项相同）；本文件上述“必须处理”条件视为已满足，计划 6 继续推进。验收状态更正：计划 1 阻断项已清除（含补遗 `90164e15` 修复 `test_policy_edit.py` 的 `waf_bypass` 断言），其余结论以复核报告为准。

## 当前状态（2026-09-05 Review 后）

历史处置记录保留；以下标签表示当前验收状态。

- [已完成] 活动代码、配置和执行链路中的旧 `penetration_test`/`waf_bypass` 语义已删除，负向测试钉已同步；Rust、Adapter 和构建链路没有引用已删除的 AI 执行模块。
- [已完成] 计划 1 的清理范围已覆盖 Python、Docker、构建脚本和测试；敏感字段不能进入日志、构建产物或 Git。
- [已完成]（2026-09-05 终态修复轮）`docker/ai/skills/ai-pen-orchestrator/` 已 `git mv` 重命名为 `docker/ai/skills/ai-denoise-reference/`（内容为去噪参考文档，与新命名一致）；检索口径升级为连字符形态 `rg 'ai[-_]pen'`，在 `ARL/` 源码、配置、构建文件中 0 命中，仅 docs 叙述文本保留（允许项口径）。
- [已完成]（2026-09-06 计划 6 第 10 批收口）测试卫生收敛与全量基线重生成：
  - 主动注入不还原清零：`test_task_finalizer.py` 改 `_api_unified_bootstrap.load_modules` 临时桩窗口 + finally 还原（引爆合跑断言的最后一例）；新工具 `scripts/check-test-hygiene.py`（每文件独立子进程运行，检测 app/app.services/app.utils/app.config/app.modules 槽位残留，并识别 load-fail 三态——Review 抓到首版裸模块名假绿 P0 已修）。**全量 149 文件真实口径：polluted=0、clean=84、load-fail=65**（65 个为宿主缺私有依赖 `xing` 等不可导入文件，不计污染但未验证——容器全依赖环境重扫挂计划 6 第 11 批）。修复过程中检出并收敛 6 个真实污染文件（`test_asset_monitor_context`/`test_expr_evaluation_golden`/`test_fingerprint_golden`/`test_service_fingerprint_registry`/`test_site_fingerprint_registry`/`test_unified_fingerprints_build`：模块顶层注入 fake 包槽位无还原，补"导入前快照 + tearDownModule 还原"钩子），并顺带修复 `asset_wih_monitor.py` 第 8 批遗留的包级 re-export 直绑（轻环境必炸，改子模块直导 + 测试 stub 对齐）。
  - 全量基线（宿主 python3.9、无 xing/arl_accel，`cd ARL && PYTHONPATH=. python3 -m unittest discover -s test`）重生成登记：**Ran 768，failures=20，errors=197，skipped=49**。归因分析：① 与第 10 批前基线（8fa5853c worktree 实测：Ran 520，17F/158E/22S）做逐名差集，64 个"新增"case 级失败全部为**模块采集修复后首次执行暴露的存量**——抽样三组（test_cdn 2F、test_task_helpers 1F1E、wih_endpoint_probe_cache 类内漂移）在基线 worktree **独立进程运行复现同等失败**，非第 10 批回归；② 残留合跑噪音机制定性：unittest loader 收集期交错——各文件 module 顶层 `from app.X import symbol` 在收集窗口绑定当时 sys.modules 里他人注入的 fake，单文件运行互现不了；根治需把顶层直接绑定改为 bootstrap 捕获引用（存量重构，不属本计划范围，移交后续专项）。
  - 口径声明：本基线为**宿主轻依赖环境**（容器内全依赖+xing+arl_accel 环境另行差异，双架构容器回归归计划 6 第 11 批）；"124 failed/348 passed/34 errors"为 2026-09-05 部分收集失败时的历史快照，已被本行取代。
- [未完成] `config-runtime.yaml` 的敏感配置生命周期只完成 Git 忽略约束；服务器同步和脱敏启动校验仍是运维门禁，禁止把文件内容写入仓库或报告。

当前判定：计划 1 的活动语义清理主体 [已完成]；仓库卫生和运行期治理 [未完成]。详见 [计划 1–5 前置收口 Review 报告](<../review/[已完成]计划1-5前置收口Review-20260905.md>)。
