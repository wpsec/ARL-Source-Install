# 03 计划3：实施批次与验收回归

## 固定批次

1. 文档和 Rust 边界冻结
2. 40 个目标性能和结果完整性基线
3. AI 渗透及废弃资产清理
4. provider 超时、重试、熔断和日志脱敏
5. 渐进式发现队列、深度队列和同 Task ID 状态语义
6. 端口扫描批次调度与 `all=0-65535` 完整性
7. 文件泄漏候选队列和站点公平调度
8. WIH 分批、周期复用和有界候选队列
9. Nuclei 模板预校验与 fallback 收敛
10. PyO3/maturin Rust URL/JS/HTML 热点
11. Python adapter、fallback 和独立 metrics
12. repository、orchestrator、stage executor 解耦
13. DomainTask/IPTask/WebSiteFetch 扫描 stage 业务服务化
14. 导出与配置中心拆分
15. 任务级 DiscoveryContext、ResponseRegistry、CandidateRegistry 和 DiscoveryLedger
16. 统一 HTTP 调度、响应复用和跨工具事件分发
17. 爬虫、URLFinder、WIH、目录扫描和服务类型路由协同
18. Rust 内容解析层接入统一上下文
19. amd64/arm64 Docker smoke、64 个目标最终对照验收

40 个目标只用于定位瓶颈和验证不会出现单阶段长时间无进度；性能最终门禁仍使用相同配置的 64 个目标，并分别执行 `RUST_ACCEL_ENABLE=false` 与 `true` 两组对照。

## 当前实施状态（2026-09-03）

- 已完成任务 repository、WIH orchestrator、统一 stage executor 拆分。
- 已完成导出查询仓储、异步导出 job 仓储入口、配置文件存储和配置持久化服务抽取；HTTP API、Mongo 文档字段和 Python 公共入口保持不变。
- ARM64 本地 Docker 定向回归：`32 tests OK, 6 skipped`；Rust native smoke、golden corpus 和生产路由导入验证已通过。
- amd64 与 arm64 已使用同一份 Rust 源码完成完整应用镜像构建、release wheel、native smoke test、golden corpus 对比和仓储回归；实际生产发布部署验收仍需单独执行。
- 64 目标完整基线仍待新任务导出提供阶段级 CPU、网络等待、内存、输入输出和结束原因字段；历史任务数据缺少这些字段，不能直接作为完整验收基线。
- arm64 与 amd64 完整镜像均已完成本地构建；MassDNS 已在 ARM64 环境内编译为 AArch64，Rust smoke、Python adapter、golden corpus、架构兼容和分层回归均已通过。四个应用容器此前使用同一 ARM64 应用镜像运行；本轮审查不重启运行中的扫描容器。
- 2026-09-03 完成架构解耦第二批及后续增量：新增 `ConfigDomainService`、`ScanConfigService`、`ServiceApiConfigService`、`AIConfigService`，并新增 `DomainTaskOrchestrator`、`IPTaskOrchestrator`、`WebSiteFetchOrchestrator`，统一配置域事务、任务终态和站点阶段顺序；保留原有 HTTP API、Mongo 字段、任务状态语义和 Python 公共函数签名。旧的配置重复实现已移除，新增 Python/Rust 双基线比较工具。静态检查、`git diff --check`、配置/编排/基线单测及 ARM64 容器定向回归通过；本批未重建或重启现有扫描容器。
- 2026-09-03 完成扫描 stage 服务化增量：`DomainDiscoveryStageService`、`DomainNetworkStageService`、`DomainSiteStageService`、`DomainPostProcessStageService`、`IPNetworkStageService`、`IPPostProcessStageService` 以及 `WebSiteDiscoveryStageService`、`WebSiteExternalScanStageService`、`WebSiteIntelStageService`、`WebSitePostProcessStageService` 已接管对应阶段组合；`TaskLifecycleService` 已接管统计、资产同步和 `task_finalize`。新增 WAF 响应头/响应体分离匹配、主流厂商画像与 CNAME DNS 证据测试。WAF、Domain、IP、WebSiteFetch、生命周期定向回归共 26 项通过；本批未重建或重启现有扫描容器。
- 2026-09-03 完成结果写回边界增量：新增 `TaskResultWriteService`，集中封装 `CommonTask` 的结果写入、替换、批量写入和 URL 清理操作；保留原字段组装、任务 scope 校验、幂等条件和 AI 去噪触发时机。新增写回服务回归，未重建或重启现有扫描容器。
- 2026-09-03 完成配置测试与结果组装解耦增量：新增 `AIPromptSopService`、`AIProviderTestService`、`ServiceApiProviderTestService` 和 `TaskResultItemService`；`api_console.py` 保留兼容代理，SOP/Provider 测试副作用和 site/fileleak/nuclei/afrog/penetration/WIH/risk 字段组装已可独立测试。新增 `WebSiteSingleStageService`、`IPSingleStageService`，可选 stage 的 fallback 必须显式声明并记录 degraded；ARM64 隔离定向回归共 48 项通过，未重建或重启现有扫描容器。
- 2026-09-03 完成具体网络 stage 服务化增量：新增 `IPPortScanStageService`、`IPSiteDiscoveryStageService`、`WebSiteFetchStageService`、`WebSiteSpiderStageService`、`WebSiteScreenshotStageService` 和 `WebSiteFileLeakStageService`；保留原端口档位、扫描参数、黑名单/地理信息补充、HTTP/HTTPS 去重、文件泄漏结果写回和 URL 清理语义。新增 ARM64 隔离回归 6 项，包含 IP 端口/站点候选、Web 获取/爬虫/截图/文件泄漏边界；与既有路由、编排和结果写回回归合计 37 项通过，未重建或重启现有扫描容器。
- 2026-09-03 完成后置具体 stage 服务化增量：新增 `WebSiteIdentifyStageService`、`IPNPOCServiceDetectionStageService` 和 `IPBruteConfigStageService`，保留站点高价值筛选、WAF 过滤、NPoC 目标集合、风险插件选择和结果写回语义；新增 ARM64 隔离回归 7 项通过，未重建或重启现有扫描容器。
- 2026-09-03 完成共享发现上下文首批接线增量（固定批次 15/16/17 首批）：`DiscoveryContext` 发布六类发现事件并登记候选（爬虫、page_intel、urlfinder、js_intel、WIH 记录/接口、目录命中、URL 探测状态迁移）；爬虫与文件泄漏目录扫描接入共享响应缓存和类别调度；WAF 阻断类别化——目录字典流量只暂停该主机 directory 队列、子进程确认证据回流上下文，非目录来源维持主机级口径；调度有界等待后 fail-open 并计 `over_limit_request_count`；响应注册中心加总字节预算；观测快照仅收尾日志，Mongo 侧仅 `waf_skip_summary.class_blocked_hosts` 一个向后兼容观测子字段。ARM64 隔离定向回归 106 项通过（新上下文 13、策略接线 7、WAF 含类别用例 15 在内；`test_asset_scope` IP 段用例确认为 HEAD 既有失败，与本批无关），`app.main` 导入验证通过。DiscoveryLedger 持久化 backend 与真实连接池收编留待下一增量。
- 2026-09-03 完成容量、启动与测试基础设施收口增量：候选图加容量上限（默认 20000、最旧驱逐、`candidate_evicted_count`，`DISCOVERY_CANDIDATE_MAX` 软读）且爬虫仅在相似去重通过时登记；`arl_update` 收口为 `app.tools.arl_bootstrap` 显式入口（start_web.sh 于 gunicorn 前调用，`import app.main` 不再要求 Mongo 可达）；Nuclei 模板预校验进 metrics、批次签名成功后去重（`duplicate_batch_skipped`）；`transfer_ip_scope`/`ip_in_scope` 支持非对齐 IPv4 区间（修复 HEAD 既有 `test_asset_scope` 失败）；修复 `test_celery_recovery`、`test_fingerprint_cache_unit`、`test_route_build_return_items`、`test_site_spider_enhance`、`test_wildcard_profile_unit` import 期替换 `sys.modules["app"]` 不恢复的合跑污染（改 setUpModule/tearDownModule 快照还原，route 加载器补 collection_query_service 替身）。35 套件 168 项单进程合跑全绿；`test_asset_scope` 不再属于已知失败清单。
- 2026-09-03 完成 P1 解耦收尾增量（批次 12/13）：新增 `IPCertStageService`（SSL 证书采集、SNI/default 去重与 cert 幂等 upsert）、`IPServiceSummaryStageService`（nmap/NPoC 服务合并与 service 表按任务重建）、`WebSiteNucleiScanStageService`（目标组装、Mongo 超时延迟重试语义原样保留、AI profile 注入与结果写回）、`WebSiteAfrogScanStageService` 与 `WihResultPersistService`（WIH 记录/接口/风险落库、范围校验、敏感提升判定、周期复用记录去重）；`IPTask.ssl_cert`/`save_service_info`、`WebSiteFetch.build_nuclei_targets`/`nuclei_scan`/`afrog_scan`/`_preview_nuclei_batch_plan` 及 `_save_wih_*` 全族改为兼容委托，`commonTask.py` 2744→2261 行；修复 `test_wih_endpoint_probe` 过时的 `_run_substage` fake 签名。`TaskResultItemService` 幂等复审完成（写语义与 HEAD 一致，补回退键稳定性测试；观察项 `wih_endpoint.fnv_hash` 回退为明文拼接，保持字段兼容不改）。新增迁移等价性回归 7 项，39 套件 180 项单进程合跑全绿。
- 2026-09-03 完成 AI-POC 规划器迁移（批次 12/13 收尾）：新增 `AIPocPlannerService`（约 1200 行：常量、索引缓存、上下文收集、模型调用、决策落地、用量日志），`run_ai_poc_scan_plan`/`_collect_ai_poc_*`/`_normalize_ai_poc_*`/`_write_ai_poc_usage_log` 等同名方法保留任务侧兼容委托，`WebSiteFetch` 常量改为服务类别名（同一对象引用）；`_handle_ai_poc_stage_degrade` 降级路径与 pass-through 语义保留任务侧。顺带修复 `_AI_POC_INDEX_CACHE` 类级初始化在上批未提交重构中丢失、索引文件命中即 AttributeError 降级的回归（恢复初始化 + getattr 防御）。`commonTask.py` 2261→1158 行。迁移等价性回归 2 项（常量别名一致性、disabled 路径日志/注入语义），39 套件 182 项合跑全绿。
- 2026-09-03 完成阶段账本持久化与首批恢复接入（批次 5/15 深化）：新增 `discovery_ledger_store.MongoLedgerBackend`（`task_stage_ledger` 独立集合、`key` 唯一索引入 `arl_update.create_index`、claim 用状态过滤原子 upsert 且崩溃遗留 fetching 可重接管、后端故障全部 fail-open 宁可重扫不漏扫）、`DiscoveryLedger.claim` 优先委托后端原子实现；文件泄漏按 `(stage=file_leak, target, dict路径, 字典内容签名)` 记录 covered，重跑命中即跳过且不派生 watchdog；WIH 主扫描按站点粒度记录，仅整批 `end_reason=completed` 且无 timeout/salvage/failed 才记账（降级批次必重扫）；周期复用（跨 run）与账本（同 task 重跑）互不干扰、复用站点仍优先排除。`TaskRepository.delete_related_data` 无条件级联 `task_stage_ledger`。账本回归 7 项 + 修正两处滞后测试断言（fake file_leak 签名、删除集合期望），40 套件 189 项合跑全绿。
- 2026-09-03 完成功能补齐批次（C 类残项代码化）：①`page_semantics` 页面语义证据（body_excerpt≤600、semantic_tags 弱形态标签）在 fetch_site/page_fetch（含缓存路径）/fileLeak.dump_json（子进程出口）落库，site/url/fileleak 文档仅增字段；②新子域同任务进入目录扫描队列（范围校验、`FILE_LEAK_NEW_HOST_MAX` 有界、消费后 queued，站点/WIH 注入维持周期语义）；③Nuclei 阶段账本（目标集合+profile 指纹，仅 success+completed 记账，重跑跳过）；④普通 HTTP 路径连接池收编（模块级共享 HTTPAdapter、每请求独立 Session 保 cookie 隔离、pooled Session 禁 close、urllib3 `_error_catcher` 保证脏连接丢弃，直连/provider 分支不变）。42 套件 218 项合跑全绿（新增语义/恢复 14 项、连接池 3 项）。功能型残项清零，剩余全部为基线数据/部署窗口/分池评审前置项。
- 2026-09-04 推进计划5 第6-7阶段代码面（`scripts/fingerprint-ruleset-diff.py` 对照工具）：legacy（Mongo+kscan 内置）vs unified（基线 gz+overlay）**生效规则集**对照——首次运行即抓出审计缺口（policy 拒绝 153 条无名单可归因），修复为 build 导出 `meta.rejected_rules_detail` 后闭环：本地预览（空 Mongo）下仅 legacy 有 11 项全部归因 `all_branches_rejected`（云盾/后台/众拓/瑞星/深信服ngaf 等 2-6 字泛化名，即误报治理的既定代价，待人工复核确认），未归因=0；仅 unified 有 15077（faviconhash/webapp/kscan_local 进链，方向可解释）。第6阶段实质已随第2-5阶段落地（local 基准进 gz、不新增扫描进程、无服务误归类）；第7阶段旧文件清理**被放行依赖阻塞**：需 x86 真实 Mongo 重跑对照 + unified 观测期通过后，才允许摘除 webapp.json/finger.json/kscan*.json 的加载路径。全家桶 8 套件回归绿。
- 2026-09-04 完成计划5 第5阶段代码面（服务识别收敛，增量式按 §零.2 口径）：新增 `ServiceFingerprintRegistry`（加载提交的 service_fingerprints.json.gz）——npoc(100)>nmap service(90)>product(80)>port(25) 优先级选举 + 冲突证据（rejected 侧不静默丢）、端口弱候选 confirmed=False 永不确认、文件缺失降级 strip+lower 透传（识别链不中断，与站点 unified 失败语义刻意不同）；接线 `IPTask._normalize_scheme`（registry 优先、硬编码表兜底）与 `_apply_npoc_service_result`（回填带证据），service 集合文档附加 service_confidence/service_sources/service_conflict 观测字段（既有字段与导出不动）。跨任务幂等键决策=不做（任务内 target_set+写库 uniq_key 已闭环，跨任务跳过协议探测有漏检风险，05 已登记）。测试 10/10（真实 gz 产物、别名 canonical、http/https 冲突、udp/tcp transport 校验、降级路径），指纹全家桶回归绿。
- 2026-09-04 完成计划5 第4阶段（Mongo/API 兼容，方案=基线+运行时合并视图）：SiteFingerprintRegistry 增加 Mongo `fingerprint` overlay（merge_key 同名并分支/新名独立成规，**用户规则豁免编译期 policy 只验格式**——body="login" 类显式意图保留）；版本联动 = `finger_db_cache.update_cache` 尾部 INCR `arl:fingerprint:unified:ver`（route 三写点覆盖 + CLI import_fingerprint 补齐），Registry 60s 检查重建、ver 不可读 600s 对账；冷启动/Mongo 故障基线兜底显式记录。修 deepcopy 污染回归（overlay merge 曾 mutate `_base_rules`，重复重建分支膨胀——测试锁死）。文档对齐：05 第4阶段改写为合并视图方案（compose 实证 dicts 非共享卷故弃"运行时重编译文件"，编译保留为 --mongo-export 发布/审计路径），登记既有限制=finger 源规则 UI 删除重启复活（legacy 语义，墓碑另立项）；unified 切换前置=x86 对照观测。overlay 测试 12/12 绿，全家桶回归绿。
- 2026-09-04 完成计划5 第3阶段代码面（commits 见 `test_site_fingerprint_registry`/`site_fingerprint_registry.py`）：新增 `SiteFingerprintRegistry`（加载 site_fingerprints.json(.gz)、sha256+mtime/size 热重载、icon_hash 精确桶仅作优先不剪枝、缺失/损坏→ok=False 显式降级 legacy——不空规则静默启动）；规则判定复用运行时同一 `FingerPrint.identify`（零新轮子），置信度直读编译产物；`fetchSite.fetch_fingerprint` 接入 `SITE_FINGERPRINT_SOURCE=unified` 分支（默认 legacy，双路径保留为显式 fallback），`finger_identify_detail` 与其共用 `build_identify_variables` 字段归一；config.py/两份 yaml 模板加开关键。测试 7/7：unified 命中集与编译基线在 12 golden 样本上严格相等、faviconhash 经规范文件链命中（摆脱 Mongo 导入依赖）、去重/降级/reload 覆盖；指纹全家桶回归绿。第4阶段（Mongo 重编译联动）完成前不得切换 unified。
- 2026-09-04 完成计划5 第2阶段（生成脚本，经用户 review 全量修正后落地，commits `d48541ba`/`4176aa5b`/`378619e8`）：①修复 `evaluate_expression` ≥3 操作数隐式 None 生产真实缺陷（连续 ||/&& 规则在运行时永不命中且无告警；golden 更新仅 2 条新增零删除，行为只增不减）；②纯函数下沉 `app/fp_common.py`，生成器与 xing/celery/Mongo 运行依赖解耦（本地 `python3 -m` 可直跑）；③生成脚本 `app/tools/build_unified_fingerprints.py`：四路源→22260 站点规则+service seed_v0（transport 含 udp 修正、resolution 消费契约、显式未接入声明），分支级拒绝（`A && login` 整分支作废，修条件级摘除扩大命中面）、括号防御、casefold merge key（修 A-B/AB 错误合并）、分支级 sources 证据、schema 校验+临时文件+last-good+原子替换+失败回滚（磁盘错误注入测试验证）、默认路径修正 tools/finger.json；④规范基线 gz 产物入 git（1.08MB，meta.input_files 全输入 sha256），解决 local 被忽略的不可复现问题；⑤语义门禁收紧为"编译命中 ⊆ 运行时命中、差集逐一归因 policy"，14 测试+指纹全家桶回归全绿。**运行时尚未接入规范文件（第3阶段），统一目标未兑现**。
- 2026-09-04 完成计划5 第1阶段（来源与接口冻结）：①`scripts/fingerprint-baseline.py` 脚本取证产出 `docs/completed/[已完成]05-附录A-指纹现状冻结清单.md`——四文件 sha/条目数（webapp 1930、finger 28884 条 15511 cms、kscan 7239、local 9673 且经核为 kscan 严格超集（kscan 独有 0）、名字交叉、weak-rule 画像（kscan 单条件泛化 1057/990 条、finger 短关键字 811 个）、quote-aware 解析后数据零断裂、regex `~=` 存量 0、配置开关 12 键现状、加载路径 git 锚点）；②golden 三段链快照 `ARL/test/test_fingerprint_golden.py` + `fixtures/fingerprints/`（12 合成样本，假包遮蔽绕开 xing/celery eager import，Mongo 空集+Redis 关，legacy→kscan/cache 合并→wappalyzer 归一，ARL_UPDATE_FINGERPRINT_GOLDEN=1 生成、默认回归门禁，1.9s），既有指纹测试（bundle/kscan_json/cache_unit）无回归。**golden 揭出的现状行为基线**：expr `=` 为大小写敏感 `in`、生产 header 走 `get_headers` 原始大小写 → kscan 小写 header 规则真实链路基本失配（静态召回实际依赖 body/title/response）；此为冻结事实，后续任何大小写不敏感化按行为变更走标注对照。待用户拍板：local bundle 是否进生产（独有 1760 条）、finger.json 与 Mongo 真相源关系。
- 2026-09-04 完成计划4 review 修复两轮（commits `bd6a299e`/`c8b9a537`，用户评审驱动）。轮1：vite.config 删除 GEMINI_API_KEY define 注入（src 零消费核实，dist 无残留）并清理 .env.example/README/metadata.json 的 AI Studio 脚手架残留；列表激活不再被 moduleListLoadedRef 钉死、改由 fetchQuery 判新鲜度，runAction 成功后 invalidateQueries(refetchType:none)，任务列表 running/waiting 存在时 15s 轮询；Suspense 外加 ViewErrorBoundary（key=模块切换重置）；Modal 自动绑定标题 aria-labelledby；行键 Math.random→稳定兜底；Dockerfile frontend_builder 无 lock 直接 fail；DataTable 接线 AI 用量日志表获得真实消费方。**附带发现**：项目原无 @types/react/@types/react-dom，tsc 对 react 全为 any（类型门禁空心），补齐后全仓 tsc 真实通过。轮2：DashboardView 残留 Math.random 行键清除（全 src 随机键清零）；全局新建任务（MainShell.executeGlobalAction）成功路径补显式 invalidateQueries + refreshSignal 信号进 TableModuleView 激活依赖，修复"已在任务页时全局建任务无可见刷新"（download 动作不触发）。两轮均 tsc+vite build 验证；Docker 多架构/Safari/Lighthouse/视觉走查仍挂账待联调窗口。
- 2026-09-04 完成计划4 第二波：Phase 0-4 代码面全部落地（commits：Phase0 骨架止血 / Phase1 daisyUI 组件层 / Phase2 模块拆分 / Phase3 数据层 / Phase4 性能，收口 rev `4b434583`）。①Phase0：表格卡纵向自滚与"首行冻结"下线（双滚动条根因，冻结实现即计划禁止的 max-h+overflow-y 自滚，属计划既定行为收敛）、19 个内联 modal 内容滚动统一 72vh 单一来源、新增 layout/PageHeader 全站页头一处定义（含仪表盘/系统监控原 6xl 标题归队）。②Phase1：daisyui@5 CSS-first 注册 brand/midnight/slate/nord/titanium/sandstone 六套主题（token 全部 var 引用现有 --brand-*，值单一来源与氛围层零改动；primary=accent-strong 决策落地）；`scripts/check-theme-contrast.py` 6 主题×12 配对 AA 全过（最低 4.58:1）；components/ui 六件套（Modal 原生 dialog/Badge/Card/FormRow/CheckboxCard/DataTable）；19 个内联 modal 全量收口 `<Modal>`（inline_modal_blocks 19→0）；App.tsx brand token 全局迁移（894→48 行）；清理 express/@google/genai/dotenv 死重；package-lock 纳入 git（解除 .gitignore 屏蔽，Dockerfile 有 lock 自动走 npm ci）。③Phase2：App.tsx 18936→439 行——八视图入 src/views、领域纯函数入 src/domain（format/task/finger/wih/system/cells/payload/types）、传输层 src/api/client、模块配置 src/config/modules、控件类 src/ui/classes；删除 14 个从未接线的占位壳组件；机械拆分行为零改动。④Phase3：main.tsx 挂 QueryClientProvider，TableModuleView 列表 fetch 收敛 fetchQuery（key=token+模块+page+size+order+筛选签名；LIVE_STATUS 模块与 forceRefresh staleTime=0，其余 30s）——模块往返重复请求消除路径就绪，写路径/轮询协议零改动。⑤Phase4：视图+ActionDialog React.lazy+Suspense，主 chunk gzip 328→142KB（预算 180KB 达成）、recharts 独立异步 chunk；DataTable >200 行虚拟滚动（@tanstack/react-virtual）。验证：tsc+vite build 全绿；headless Chrome 登录页截图（sandstone 主题/氛围层/按钮渐变渲染正常）；附录A 消费面重扫 135 条与基线一致。待办：x86/arm64 容器构建、视觉走查、Safari dialog 键盘项、TTI/Lighthouse 实测（均需联调窗口）。
- 2026-09-04 完成计划4（UI 体系重构）首批：Step1 契约冻结 + Step2 基线采集。①新增 `scripts/freeze-api-contract.py`（AST 提取后端 routes × 前端消费面），产出 `docs/completed/[已完成]04-附录A-API契约冻结清单.md`——154 端点/38 命名空间/UI 消费 135 条/37 模块列与动作契约/列表标准信封 `{page,size,total,items,query,code:200}`，重构期间行为只读基准，后端契约确需变更时先改代码重跑脚本再改 UI；②`scripts/collect-ui-baseline.sh` rev `83bb0623` 实测：App.tsx 18936 行、components 16 文件、src 19859 行、brand 内联 894 行、overflow 64 处、inline modal 19 处、useEffect 42 处、fetch 3 处、最大 chunk 1197046B（gzip 328KB），与 docs/04 计划表数值逐项一致，脚本复核通过；③启动前提与遗留决策经用户确认：x86 smoke 已完成、冻结窗口解除，primary=accent-strong 原值，Phase 2 真实路由组件拆分，Step 1 采用静态契约清单。TTI/Lighthouse 因需后端在线环境，待联调窗口实测补录。
- 2026-09-04 完成第六批：x86 真实环境（11 任务 hw1 批次）日志诊断驱动的修复。诊断确认第四轮修复全部实锤生效（children_cpu 指标、waf_block 类别账本与重启回灌、directory_class 隔离、编排时序、utibet 长任务正常运行非卡死）。据日志修复两项：①`wih_orchestrator` covered 记账防护——xzass.org 案例中 Go WIH 撞 403 墙"成功返回"仅 2 条记录 0 endpoint，3 站点仍被记 covered，重投将永久跳过 WIH；现零产出批次与 wih 类 blocked 站点一律不记账（保留重投重试），补 3 项 fake 用例锁定（含 metrics 全绿形态）；②`host:` 空端口脏值双向收敛——DomainInfo 构造与 probeHTTP 目标构建均 strip 尾冒号，消除 `https://host:/wp-json/...` 非法 URL 与响应缓存 key 分裂。两项误报澄清：site 状态码字段实名 `status`（查询侧口径错）；task 文档无 create_date 但新版 UI 无消费方，暂不补。同批删除孤儿 `cloud_security_scan.py`（唯一入口随渗透链消失，用户确认）。
- 2026-09-04 完成第五批：UI 调整与 AI-POC/渗透链整体移除（用户决策，反转 docs/01 原"AI_POC 保留"结论）。UI 面：①新建任务表单 17 项扫描功能默认勾选（DNS字典智能生成/测绘引擎查询/ARL历史查询/端口扫描/服务识别/OS识别/SSL证书/跳过CDN/站点识别/搜索引擎/爬虫/目录扫描/nuclei/afrog/Host碰撞/WIH/跳过WAF；截图、通知、暴力破解保持不勾）；②WIH 接口列页主列由页面URL改为接口URL（url 字段，page_url 保留在详情与搜索）；③endpoint 探测 observed 文案改为"引擎运行期已捕获该响应（状态码 N），未重复探测"；④PoC风险列页删除"风险URL"列与搜索项，nuclei_result 与 vuln 新增"查看详情"弹窗（关键字段网格+验证报文全文复制）。代码面：整体删除 `ai_poc_planner_service.py`/`build_poc_index.py`/`poc_index.json`、`penetration_scan.py`/`penetration_request_policy.py`、`strip_disabled_penetration_options` 强制关闭机制及 `waf_bypass` 派生属性（AI-POC 读配置引用不存在的 `_extract_ai_config`、开关事实上恒开，一并随链消失）；commonTask 别名/委托/runtime 字典/degrade 处理器全部退场；nuclei/afrog 消费点回落默认参数路径（与 disabled pass-through 行为等价）；`AI_POC_SCAN_ENABLE` 从 4 份 yaml、ai_config_service、api_console scene、前端表单/开关/文案清除；AI 去噪、WIH 接口 AI 填充、AI 接口测试三条保留能力零改动。注意：第四轮批次④中 penetration_scan 吞异常计数的落地点随模块删除一并消失（改造价值归零，属决策变更的正常覆盖）。遗留处置：`cloud_security_scan.py` 唯一入口随 penetration 阶段消失成孤儿，经用户确认已一并删除。验证：tsc + vite build 通过；受影响后端回归 76 项全绿（2 项失败为已知 py3.9 环境差异，容器 py3.10 下不复现）；镜像与 UI 变更需 x86 重建后验收。
- 2026-09-04 完成第四轮统一发现架构验收差距修复批次（12 项逐条核实，落地 9 项）：①阻断修复：WIH 记录入候选图的 `recordType` 属性错位（`getattr(record,"record_type")` 恒空、新子域/新 API 静默不分发），双形态兼容 + 真实属性形态回归锁定（旧代码必挂）；全仓同类读法排查无误用。②`await_singleflight_leader` 等待上界收敛为 `max(1s, min(10s, 阶段剩余预算))`（挂 `provider_http.current_stage_remaining_sec`，先例同 dns_query），慢响应不再拖满 10s 后重复发请求。③阶段资源指标补子进程口径：`RUSAGE_CHILDREN` CPU 差值（`children_cpu_elapsed_sec`）与已回收子进程 RSS 历史峰值（`children_rss_peak_mb`+scope 标签），不改写既有 `cpu_elapsed_sec` 字段语义。④吞异常观测化（安全语义优先）：penetration_scan 10 处漏洞测试请求异常接入 `probe_failure_counts`+收尾 warning 汇总（"探测失败"与"无漏洞"可区分）；`task_scope_guard` 两处 DB 吞异常补 warning（scope 静默缩小可见）；`CandidateRegistry` 驱逐回调吞异常补计数并暴露到观测快照。⑤`urlfinder_sensitive_scan` 接入 `discovery_context` 与账本目标级恢复：选批前跳过已 covered 目标、批次完整成功逐目标记账（worker 重投不重复二次扫描）；**否决**评审原建议"缓存命中即跳过目标"——敏感规则由 Go 引擎抓取时执行，缓存 body 从未过规则，跳过即漏检。⑥fileLeak 子进程响应回传：result.json 增 `responses`（命中页 ≤64 条、body≤50KB base64），父进程 `_apply_child_responses` 以独立 `file_leak_get` profile 登记（不污染 html_get 语义），子进程与 inline 回退两路径同挂；目录请求跨进程租约仍缺（见遗留）。⑦候选图断链接线：`urlfinder_url_probe._collect_candidates` 消费 `url/page` 候选（discovered/queued 态），补上 `UrlCandidateDiscovered` 零消费断链；探后状态迁移既有逻辑复用。⑧WAF 熔断跨重启持久化：`record_signal` 输出 `newly_blocked` 迁移标志，首次阻断落 `task_stage_ledger`（`waf_block|host|class` 键，随任务级联删除清理），context 重建时 `restore_waf_state` 回灌（绕开事件重放与重复落账），commonTask 构造点接线；MongoLedgerBackend `list_pending` 泛化为 `list_by_prefix`（旧名保留别名）。⑨`DiscoveryEvent` 补一级 `task_id`/`input_signature`（尾部默认值字段，三构造点注入）；缓存指标口径拆分：`cache_miss_count`（原 unique，语义澄清注释保留）、`network_request_count`（scheduler granted）、`actual_duplicate_request_count`（put 时 key 已存在=真实重抓），`cross_strategy_reuse` 判定迁到记录级 consumers，删除永不清理的全局 `_response_consumers` 污染源。本轮同批核实中修正评审两处口径：事件"发布无订阅"属实但 endpoint 已有 followup 轮询消费者；"context 全部丢失"不属实——账本 covered 与 overflow 候选本就是恢复通道，真实丢失面（响应缓存、WAF 熔断、容量内候选）中 WAF 熔断本轮已持久化。**验收缺口挂项（64 目标/amd64 发布前必须给出处置）**：(a) provider 级硬取消/cancel token——现阶段仅有 IO 层 deadline 主动收敛，纯计算/未接 deadline 的环节（port_scan 等）拖预算无解，需专项设计；(b) Go WIH 子进程消费统一缓存——需改外部二进制输入协议（stdin/文件喂响应），本仓库无 Go 源，属仓外依赖，验收时以 `external_network: wih_go` 标记口径单独评估重复请求率；(c) `ResponseRegistry` 响应体持久化——48MB 任务内存在态，Mongo 全量落 body 成本高于收益，维持不持久化决策（WAF 熔断与候选恢复面已各自有通道）；(d) fileLeak 目录请求跨进程 scheduler 租约（IPC 协议或父进程代理），当前 directory 全局限流仅覆盖父进程准入；(e) `PageFetched`/`SiteDiscovered` 等剩余事件的生产订阅者按策略需求逐个立项。44 套件本地合跑中改动路径专属回归全绿；89 项既有失败为环境差异（无 Mongo/Rust/wih 二进制/py3.9 staticmethod），最终验收仍须在 ARM64/amd64 容器执行。
- 2026-09-04 完成第三轮 review 修复批次（10 项断言逐条核实后落地 4 项、更正误报 2 项、口径统一 1 项、显式边界 2 项、遗留 2 项）：①核实"task_lifecycle 删 stat_cip 写 cip"为误报——删/写同为 `cip` 集合且 round3 回归已锁定，不动；②`discovery_ledger_store.finish` 修复确认查询与更新耦合的 fence 绕过洞：`find_one` 移出 update try 块单独容错，确认失败时拒写（拒写不阻断扫描）而非退回无条件覆盖写，仅"归属仍是自己/无记录"才整条覆盖，补 3 项异常路径回归；③endpoint 探测由"主动语义不复用缓存"更正为全量接入统一响应消费：GET 共用 `html_get` profile、POST 按 method+body 摘要独立 profile、miss 走 single-flight、成功响应回填 registry，blocked 拒发路径同步释放槽位，新增 5 项缓存回归；④WIH 编排时序修正：`wih_endpoint_probe/ai_fill` 移至 `wih_urlfinder_extract` 之后（同页探测直接复用 URLFinder 缓存，不再二次请求），并在情报链末端新增 `wih_endpoint_followup_probe` 二次消费候选图中未探测的 endpoint 候选（GET-only、上限沿用 `WIH_ENDPOINT_PROBE_MAX_TARGETS`、探测后置 fetched），补编排时序/二次消费回归；⑤`stage_executor` 异常分支补超预算证据（elapsed vs budget 写入 finish_stage metrics 与失败日志），"硬中断所有 provider"评审为不现实项维持 deadline 主动收敛现状；⑥Go WIH 子进程（主扫描+二次敏感扫描）与 fileLeak 目录子进程两处外部边界在 02 文档显式登记为按设计保留，待 Go 输入协议或 IPC 租约专项；⑦00/02 文档统一：AI-POC"继续迁移"与"已完成"矛盾句、`DiscoveryLedger` backend 历史口径全部修正并标注修订时点；⑧遗留：统一响应缓存指标口径拆分（cache_miss/network/actual_duplicate）与 `DiscoveryEvent` 补 task_id/input_signature 记入下一增量（00"后续实施顺序"已挂项）。
- 2026-09-03 完成第二轮 review 修复批次（3 阻断/6 高风险/4 一般全部落地）：①`discovery_ledger_store.claim` 改单次原子 `find_one_and_update`+owner token+租约，仅 pending/终态/过期 claiming 可接管、covered 永不可抢、DuplicateKey 竞争判负、后端故障 fail-open；②`DiscoveryContext` single-flight（`await_singleflight_leader`，槽位由 `put_response` 统一释放），fetchSite/pageFetch/爬虫/fetch_text/urlfinder_extract/favicon 六站并发 miss 合并，等待上界 10s；③`NewHostQueue` 订阅 `NewHostDiscovered`→有界队列→WIH 主扫描注入 + `add_scope_host` 动态扩 WAF 观测（空 scope 不追加防收窄；站点采集队列注入维持下一轮周期语义并已在 02 注明）；④WAF 升级收敛：weak 状态码证据只暂停来源类别、strong 证据才主机级阻断，sink 携带 block_scope；⑤favicon 复用原始页面正文 + `favicon_get` profile 缓存，消除每站对原页重复请求；⑥api_doc_scan 接入共享 fetch_text、wih_endpoint_probe 接入 wih 类别租约（blocked→可见 skipped；探测保持主动语义不复用缓存），Go WIH 子进程明确登记为任务内缓存边界外；⑦`stage_executor` 超预算强制降级（success→partial/budget_exceeded，error/partial/pending 保留）；⑧config-runtime.yaml 的 `AI_PEN_*` 11 行已删（01 验收闭环），`.gitignore` 补运行产物；⑨Dockerfile：ARL-NPoC 安装 fail-fast + `import xing` 构建期校验（镜像实测通过）、删除 requirements 已覆盖的冗余静默行、wih 冒烟 `|| true` 改 `test -x`。新增 round2 回归 15 项；43 套件 235 项合跑全绿。

- 2026-09-05 完成终态语义与统一候选 drain 修复批次（Review §4 重要项1/2、外部边界标记、计划1 目录命名收口）：①`TaskStatus` 新增 `done_pending`/`done_degraded` done 家族终态，`DomainTaskOrchestrator`/`IPTaskOrchestrator` 改为消费 `TaskFinalizer.terminal_status()` 决策，阻断口径=WIH 队列残余；后端全部终态守卫（celerytask 分发/清理 `$nin`、utils 孤儿回收与 interrupted、routes 停止/删除/同步/重启、计划汇总、完成通知、collection 查询 `status=done` 过滤、console 文案、前端 normalize/进度/终态判定）同步家族兼容映射；周期复用基线保持只认裸 done。②策略级候选消费协议：目录扫描以 `file_leak|<target>` covered 账本为消费证据，晚到 host/url/page/endpoint 候选显影 `pending_backlog|directory|*`/`url_probe|*`/`api|*`，队列容量丢弃主机显影 `wih_overflow`（自查轮补，非阻断）；`TASK_FINALIZER_PENDING_MAX` 限账本写入量、范围过滤防虚增；js/path/site 候选观测移交计划 6 第 3 批。③修复收尾 drain 调用不存在入口 `web_info_hunter` 的真实缺陷（生产方法为 `run_web_info_hunter`，异常吞噬导致 drain 从未生效），入口缺失时残余显影降级不静默；并修复残余双计数。④`external_network_wih_go/trufflehog/file_leak_directory` 上下文指标统一记账并透出到收尾 stage metrics（任务与监控两路径）。⑤`docker/ai/skills/ai-pen-orchestrator/` 重命名为 `ai-denoise-reference/`，检索口径升级 `rg 'ai[-_]pen'`（源码/配置/构建 0 命中）。验证：定向本地 39 项通过（原 21 项收口测试 + 修复轮/自查轮新增 18 项：终态决策/入口解析/策略 pending/wih_overflow 显影/映射一致性）；真实编排器代码经依赖桩驱动完成 Domain/IP/Web 三终态与顺序验证；`py_compile` 全量通过、`git diff --check` 通过、前端 `tsc --noEmit` 通过（dist 重建挂计划 4 门禁）。本批未重启容器；容器全量回归、真实配置 smoke、40/64 目标性能、amd64 发布、测试 fake 卫生维持挂账。详见 [终态语义与统一drain修复记录](<../review/[已完成]终态语义与统一drain修复记录-20260905.md>)。

## 每批要求

- 先删测试和文档冲突项，再删实现
- 每批必须可独立通过测试
- 每批不引入新的兼容策略
- 每批结束都要执行全文检索，确认无新增历史残留
- Rust 批次不允许修改 Mongo 文档结构、HTTP API 或现有 Python 公共函数签名
- Rust 单批异常必须有明确日志和 Python fallback，不得静默吞错
- 解耦批次必须保留兼容入口，先验证阶段状态、异常收尾、结果回写和重试，再删除任务类中的重复实现
- 共享上下文批次必须先验证请求去重、候选跨策略传播、来源聚合和 WAF 流量隔离，再接入 Rust 或扩大网络并发
- 同一任务内响应缓存只能按请求 profile 复用；只发现但未完成处理的候选不能被误判为已覆盖

## 对外接口与配置变更

删除接口：

- 旧 `/ai_pen_test/` 全部接口
- 配置中心中所有 AI 渗透执行相关接口

删除配置：

- 全部 `AI_PEN_*` 配置项
- `AI_PEN_EXTERNAL_TOOL_DIR` 及相关外部工具配置

新增配置：

- `RUST_ACCEL_ENABLE`，默认 `true`
- `RUST_ACCEL_FALLBACK_ENABLE`，默认 `true`
- `PROGRESSIVE_SCAN_ENABLE`，默认 `true`
- `SEARCH_PROVIDER_CONNECT_TIMEOUT_SEC`、`SEARCH_PROVIDER_READ_TIMEOUT_SEC`、`SEARCH_PROVIDER_RETRY_MAX`
- `SEARCH_PROVIDER_STAGE_TIMEOUT_SEC`、`SEARCH_PROVIDER_CONCURRENCY`、`SEARCH_PROVIDER_CIRCUIT_BREAKER_THRESHOLD`
- `SEARCH_PROVIDER_PROXY_FALLBACK_ENABLE`
- `PORT_SCAN_BATCH_CONCURRENCY`、`PORT_SCAN_BATCH_TIMEOUT_SEC`
- `SEARCH_ENGINE_PAGE_INTERVAL_SEC`、`SEARCH_ENGINE_EXPANSION_INTERVAL_SEC`

保留但必须复核：

- AI 去噪配置，确保不再依赖旧 AI 渗透链路
- `nuclei / afrog / NPoC` 当前有效扫描配置
- `AI_POC` 相关能力，确认未误删生产依赖

## Rust 接口与构建验收

- 新增 `ARL/native/arl_accel` PyO3/maturin 模块，提交 `Cargo.lock`，不提交 `target/` 或 wheel 缓存。
- Rust adapter 使用批量输入页面文本、站点范围、已有记录和扫描选项，返回规范化记录与候选目标。
- Python 保持 `run_urlfinder_extract`、`run_urlfinder_sensitive_scan` 等现有入口，Rust 不直接访问网络、数据库、队列、浏览器或外部扫描器。
- URLFinder、WIH 和页面情报阶段必须优先消费 `DiscoveryContext` 已获取的响应，不能各自重新请求相同 URL。
- 目录扫描必须使用独立 `directory` 流量类别和 WAF 熔断器；目录类阻断不得直接改变 `normal/crawler/wih` 状态。
- 爬虫发现的新子域和 URLFinder 发现的新 API 必须在同一 Task ID 内进入对应后续队列，并保留来源集合。
- 生产镜像以 Python 3.10+ 为标准；ARM64 与 amd64 构建和导入 smoke test 均已通过；旧 Python 3.8 ARMWorker 不纳入本轮验收。
- Rust 构建失败必须阻止生产镜像发布；运行时 fallback 仅作为可观测的功能保护。

## 功能回归清单

- 域名任务正常
- IP 任务正常
- `WIH` 正常
- 周期任务 `WIH` 的 `fast_mode`、复用、升级、回退和超时状态可追踪
- `wih_primary_scan` 与 `wih_urlfinder_sensitive` 的预算和结束原因符合配置
- WAF 跳过的实际生效阶段、统计口径和耗时展示准确
- PoC 风险扫描正常
- 导出正常
- 配置保存正常
- 任务停止与删除正常
- `nuclei / afrog / NPoC` 结果入库与查询不回退
- URL/JS/HTML Rust 热点结果与 Python 基线的规范化记录集合一致
- Rust 候选排序、去重、范围过滤和 hash 结果不减少有效发现

## 稳定性回归清单

- Celery 任务状态流转正常
- 导出 job 行为不回退
- 配置热刷新不回退
- 启动流程不再因导入副作用执行修复逻辑

## 性能回归清单

- 64 个代表性目标至少执行冷启动和热缓存两轮。
- 记录 CPU 时间、网络等待、内存、阶段耗时、输入规模、结果数量和 fallback 次数。
- Rust 候选热点 p95 CPU 时间降低至少 30%，或吞吐达到 Python 基线的 1.5 倍。
- Rust 接入后端到端阶段耗时不得较基线恶化超过 5%。
- 若 CPU 不是主要耗时来源，不以 Rust 化作为强制验收条件，并保留基准结论。
- 首批可用域名、IP、站点结果 p95 在 5 分钟内可见；最终 `done` 只能在深度队列清空后出现。
- provider、端口批次、文件泄漏、WIH 和 Nuclei 的失败必须显示为明确的 `failed/degraded/pending`，不能以空结果表示成功。
- `test/top100/top1000/custom/all` 端口范围与用户选择一致；`all` 的最终端口集合必须完整覆盖 `0-65535`。
- worker 具备 `CAP_NET_RAW` 时端口扫描实际采用 `nmap -sS`；权限错误才允许按批次回退 `-sT`，并验证 fallback 原因、次数和当前批次范围。
- `all` 模式按单 IP 批次执行，默认 4 路且受控并发不超过 4；批次完成、失败和待处理数量可从阶段指标复算，后续批次不会因前一批失败而被静默跳过。
- CDN IP 默认跳过端口扫描但仍保留 IP/域名资产和业务域名 HTTP/HTTPS 探测；疑似全开放主机保留原始端口集合，并不对全部端口重复执行服务识别。
- 站点探测回归必须覆盖公网 DNS 视图漂移的受限例外、私网漂移拦截和未确认域名拦截，并验证候选数、DNS 例外数、请求数和失败数可从日志复核。
- 40 个目标重点核对 `port_scan`、`dns_query_plugin`、`search_engines`、`file_leak`、`wih_primary_scan`、`wih_urlfinder_extract`、`wih_url_probe` 和 `nuclei` 的阶段指标，之后再进行 64 目标门禁。

基线保存：将任务接口导出的 JSON 文档列表保存后，执行
`python3 ARL/app/tools/collect_wih_baseline.py --input tasks.json --output wih-baseline.json`。
工具按 `stage_kind=aggregate` 排除父阶段重复耗时，并保留每个阶段的 p95 耗时、CPU/非 CPU 耗时、峰值 RSS、输入输出数量、状态和结束原因。
生成基线后执行 `python3 ARL/app/tools/validate_wih_baseline.py --input wih-baseline.json`，校验 64 目标至少两轮、WIH 阶段预算和可观测字段是否齐全。

两组开关对照完成后，分别生成 Python 和 Rust 基线，再执行：
`python3 ARL/app/tools/compare_task_baseline.py --python python-baseline.json --rust rust-baseline.json`。
工具默认比较 `wih_urlfinder_extract`、`wih_urlfinder_sensitive` 和 `wih_url_probe`，会输出 p95 CPU、估算吞吐比、端到端 p95、平均结果数和 Rust fallback 次数；也可通过重复 `--stage` 指定实际完成的热点阶段。工具报告只能作为离线门禁依据，不能替代相同目标、相同配置和真实运行环境的对照执行。

## Rust 测试清单

- Rust 单元测试：URL 归一化、相对路径、协议相对 URL、跨域过滤、模板路由、静态资源过滤、JS 递归、重复记录和异常输入。
- Python golden corpus：Rust 与 Python 输出的规范化记录集合逐项对比；固定样本位于 `ARL/test/data/rust_accel_golden_corpus.json`，使用 `python3 ARL/app/tools/compare_rust_python_corpus.py --input ARL/test/data/rust_accel_golden_corpus.json` 执行集合、去重和缺失结果校验，排序差异只有在明确使用 `--strict-order` 时才判失败。安装 native wheel 后追加 `--run-native`，直接执行 Rust 函数并与 Python golden 输出比较。
- Python adapter 测试：模块缺失、加载失败、参数错误、单批运行异常以及包含阶段、批次、fallback 原因、网络等待和请求数量的指标。
- Docker smoke test：amd64 与 arm64 镜像内均执行 `ARL/native/arl_accel/smoke_test.py`，验证 Rust 模块导入、URL/JS 提取、跨域过滤和候选排序；可使用 `bash ARL/docker/verify-multiarch-rust.sh` 分别构建两种架构并运行同一 smoke test，该脚本只构建和本地运行，不执行 push。

## 发布策略

- 本轮是收缩式改造，不做双轨并行。
- 删除项一律直接退场，不做新旧双写。
- 本轮默认不执行远程同步和自动提交发布动作。
- 本轮不重写现有 Go 版 `tools/wih`；是否迁移由独立性能基准和后续计划决定。

## 前置复核结论（2026-09-05）

计划 3 的批次记录和回归清单较完整，但最终验收仍未完成：

- 64 目标性能门禁、Rust/Python 对照、首批结果 p95、端到端阶段耗时和 amd64 发布验收仍需真实任务证据。
- 任务终态目前由编排器固定流程后的直接写入保证，尚未由统一收尾器证明深度队列清空后再标记 `done`。
- 本机定向测试无法替代标准容器回归：本次 98 项中 9 项错误、16 项跳过，原因主要是运行环境依赖和配置 bootstrap 不完整。
- 端口扫描 all 模式的批次大小和并发配置存在 Compose/runtime YAML 漂移，性能测试前必须统一配置来源。

详细报告：[计划 1–5 前置复核报告](<../review/[已完成]计划1-5前置复核报告-20260905.md>)。

## 当前状态（2026-09-05 Review 后）

- [已完成] 计划 1–5 前置收口的提交记录、针对性回归和配置声明收敛已入库；本轮新增 21 项收口测试通过。
- [已完成] 统一发现上下文、响应复用、候选账本、WAF 隔离和 TaskFinalizer 的代码级前置项已有证据；这只代表代码面收口，不代表发布门禁通过。
- [已完成]（2026-09-05 终态修复轮）`pending/degraded` 与 `done` 的兼容映射落地：done 家族终态（done/done_pending/done_degraded）+ 全后端守卫同步 + Domain/IP/Web 三类编排器终态测试；定向本地 39 项通过（含自查轮 wih_overflow 补口径）、真实编排器代码桩驱动三终态验证通过、`tsc --noEmit` 通过。详见 [终态语义与统一drain修复记录](<../review/[已完成]终态语义与统一drain修复记录-20260905.md>)。
- [未完成] 上述修复的容器侧证据：标准 ARM64/amd64 容器内的 `test_task_orchestrator`/`test_web_site_fetch_orchestrator`/`test_wih_orchestrator` 等依赖 xing/Mongo 的套件尚未执行；孤儿回收与分发守卫的 done_* 行为需容器回归复核。
- [未完成] 全量标准容器回归、服务器实际 runtime 配置脱敏校验、40 目标协同回归、64 目标双基线、首批结果 p95、Rust/Python 对照和 amd64 生产部署尚未完成。
- [未完成] 计划 4 的双架构 UI smoke、Safari、视觉/Lighthouse 和 UI 测试基建仍挂账；计划 5 的 unified 生产切换按决策暂停。

当前判定：前置实施批次 [已完成]；最终回归与发布验收 [未完成]。详见 [计划 1–5 前置收口 Review 报告](<../review/[已完成]计划1-5前置收口Review-20260905.md>)。
