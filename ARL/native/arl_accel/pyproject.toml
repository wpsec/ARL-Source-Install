[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "arl-accel"
version = "0.1.0"
description = "Rust CPU acceleration layer for ARL URL and JS processing"
requires-python = ">=3.10"

[tool.maturin]
bindings = "pyo3"
module-name = "arl_accel"
