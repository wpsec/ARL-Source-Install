# 更新日志

本文件记录 `newUI` 分支的重要变更。  
每次更新版本号时，请同步补充一条记录（日期、版本、核心变更）。

## v3.0.20 - 2026-03-11

- 策略配置（新建/编辑）中统一收敛域名爆破语义：提交时固定 `domain_config.domain_brute=true`
- 策略界面“域名爆破类型”文案调整为“默认字典模式”，并明确仅在任务未指定字典时生效

## v3.0.19 - 2026-03-11

- 新建任务页面隐藏“域名爆破”勾选开关，减少与字典配置的重复理解成本
- 提交任务时前端固定 `domain_brute=true`，保持原有扫描行为一致

## v3.0.18 - 2026-03-11

- 任务管理新增异常详情交互：`status=error` 时状态文本可点击，弹窗展示异常信息
- 前端新增异常详情弹窗，支持查看异常时间、阶段、说明与 traceback 日志
- 后端新增 `append_task_error` 统一异常落库工具，记录 `last_error` 与 `error_logs`
- 域名/IP/风险巡航/监控/GitHub 等任务入口异常处理切换到统一落库逻辑，提升排障可观测性

## v3.0.17 - 2026-03-11

- 新建任务新增端口扫描 `custom` 选项，支持录入自定义端口范围
- 前端新增自定义端口输入框与格式校验（仅允许端口或端口段，如 `80,443,10000-10100`）
- `/task/` 接口新增 `port_custom` 参数处理：当 `port_scan_type=custom` 时执行服务端校验并标准化保存
- 文档更新：README 增补“端口扫描自定义”变更说明

## v3.0.16 - 2026-03-11

- 新建任务文案统一：`domain_dict` 展示名调整为“域名爆破字典”，避免“任务级”歧义
- 交互优化：当选择域名爆破字典时，默认字典模式在界面上自动禁用并明确提示不生效
- README 更新：同步字典选择与默认模式生效规则说明

## v3.0.15 - 2026-03-11

- 新建任务新增 `domain_dict` 字段：支持域名爆破字典选择，不选时沿用配置管理默认字典
- `/task/` 接口新增 `domain_dict` 参数校验，避免提交不存在的字典路径
- 域名任务执行流程增强：优先读取所选域名爆破字典，缺失时回退 `domain_brute_type` 对应默认字典
- README 字典持久化说明补充“域名爆破字典选择”使用方式

## v3.0.14 - 2026-03-11

- 新增宿主机持久化字典目录：`ARL/docker/dicts/domain`、`ARL/docker/dicts/file_leak`
- `docker-compose` 为 `web/worker` 增加字典目录挂载与 `ARL_DOMAIN_DICT_CUSTOM_DIR` / `ARL_DOMAIN_DICT_UPLOAD_DIR`
- 配置管理扫描项增强：自动收集内置/自定义/上传目录中的域名字典
- 扫描配置增强：读取与保存时自动兼容旧字典路径，并对所选字典文件做存在性校验

## v3.0.13 - 2026-03-11

- 字典目录重构：新增 `ARL/app/dicts/domain` 与 `ARL/app/dicts/file_leak`
- 域名字典迁移：`domain_2w.txt`、`domain_dict_test.txt`、`altdnsdict.txt`
- 敏感目录字典迁移：`file_top_2000.txt`、`file_top_200.txt`、`file_test.txt`
- 新增旧路径兼容逻辑：历史配置仍可自动映射到新目录，避免升级后配置失效

## v3.0.12 - 2026-03-11

- 统一删除确认交互：全系统删除按钮改为统一的前端确认弹窗
- 优化错误/提示消息展示：过滤接口返回中的 `<script>` 与 HTML 标签，避免原样显示
- 更新前端构建产物并同步到 `ARL/docker/frontend`

## v3.0.10 - 2026-03-11

- SSL证书采集能力增强：支持协议、加密套件与加密强度采集
- 任务详情与资产搜索中的 `SSL证书/CERT` 增强展示协议与套件摘要
- 导出报告新增 `SSL证书` 工作表，并纳入钉钉知识库写入顺序
- 钉钉集成新增 `SSL证书扫描通知` 配置，支持证书临期告警通知
