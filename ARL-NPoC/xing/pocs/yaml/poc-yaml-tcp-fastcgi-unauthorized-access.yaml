id: poc-yaml-tcp-fastcgi-unauthorized-access
info:
  name: Fastcgi 服务未授权访问漏洞
  severity: high
  tags:
  - 未授权访问
  finger: fastcgi
scheme: fcgi
port: 9000
tcpRequests:
- expression:
  - set_var("details", check_fastcgi_unauth(conn))
variables: {}
transport: tcp
