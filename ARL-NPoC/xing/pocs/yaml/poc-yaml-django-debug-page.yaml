id: poc-yaml-django-debug-page
info:
  name: Django DEBUG页面信息泄露漏洞
  severity: medium
  tags:
  - 信息泄露
  finger: Django
  osprey-id: vb_2019_0031
requests:
- method: GET
  path: /lkajz123z/
  headers: {}
  expression: 'contains(body, "Django") && contains(body, "DEBUG = True") && contains(body,
    "<title>Page not found at") && contains(body, "lkajz123z/</title>")

    '
variables: {}
transport: http
