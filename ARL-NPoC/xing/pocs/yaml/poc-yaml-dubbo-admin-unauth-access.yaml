id: poc-yaml-dubbo-admin-unauth-access
info:
  name: Dubbo-Admin 未授权访问漏洞
  severity: high
  tags:
  - 未授权访问
  finger: Dubbo
  osprey-id: vb_2019_0074
requests:
- method: GET
  path: /index.html
  expression: 'status_code == 200 && contains(body, "dubbo") && contains(body, "<a
    href=\"services.html\">Services</a>")

    '
variables: {}
transport: http
