id: poc-yaml-tensorboard-unauth-access
info:
  name: TensorBoard 未授权访问漏洞
  severity: high
  tags:
  - 未授权访问
  finger: TensorBoard
  osprey-id: vb_2022_0061
requests:
- method: GET
  path: /data/plugins_listing
  expression: 'status_code == 200 && contains(body, "scalars") && contains(body, "loading_mechanism")
    && contains(body, "custom_scalars")

    '
variables: {}
transport: http
